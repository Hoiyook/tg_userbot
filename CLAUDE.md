# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

This is a Telegram userbot built on Telethon, structured as a standard flat package (`tg_userbot/`, 34 subsystem modules — see the Module index in Architecture). The repo root keeps only a thin run shell, `tg_userbot_final.py`, so the run command below and the `.claude` pkill rules stay keyed to that filename; the pre-refactor single file is preserved for rollback as `tg_userbot_final.py.bak_20260905`. It supports both Android/Termux and macOS/desktop (auto-detected via `is_termux()`) and watches the user's own **Saved Messages** ("me" chat):

1. Any media/file sent to (or forwarded into) Saved Messages is downloaded to `SAVE_FOLDER/<forward-source>/`. Retained media copies stay in Saved Messages (never auto-cleaned).
2. Any Douyin (抖音) or Instagram link sent to Saved Messages is relayed to the parse bot `@DouYintg_bot`; the bot's first direct-video reply is auto-forwarded (via the whitelist, item 4) into Saved Messages and downloaded from that retained copy under the generic naming — no `Douyin/`/`Instagram/` subfolders, no "下载 MP4 HD" button click.
3. Program-generated messages (commands, link relays, notifications) in Saved Messages are auto-deleted on an interval; real media (including the copies above) is never deleted.
4. Media (video/photo/file) arriving in any chat on the download whitelist (managed with `/wl`, e.g. the @DouYintg_bot private chat) is **forwarded to Saved Messages**, then downloaded from that retained copy into `SAVE_FOLDER/<source>/`. Whitelisted chats only get media relay — no commands, no platform-link parsing. The copy is a real `forward_to`, not a self-authored `send_file` copy: a forward's copy carries the system **「转发自 <来源>」 header**, which the user reads in Saved Messages to see the provenance and the target folder, and the download folder is resolved automatically from the copy's `fwd_from` at download time (`resolve_download_source()` → `get_forward_source()`) — re-forwarded albums keep landing under their true origin (e.g. `祂录（3D区）/`). Known trade-off (chosen by the user): a source message that carries inline buttons keeps those buttons on the forwarded copy, because Telegram forbids editing a forwarded message so they can't be stripped afterwards (live-verified: `EditMessageRequest` fails on forwarded copies). A forwarded copy can't gain a caption either, so **album members that carry no caption don't inherit text on the message itself** — instead the group caption is read once at relay time (`_maybe_album_caption` → `_fetch_group_caption`): a mixed media+photo album only carries text on one member, fetched from a ±10-id sibling window in the source chat and run through `pick_group_caption_text`; it is stored on the queue record as `album_caption`, and the download applies it as the naming caption — so grouped photos fall to `日期 相册标题 …` names instead of `photo_时间戳`. Source/folder resolution and caption inheritance are best-effort — failure only logs, never blocks the download. Albums are relayed as one unit, not member-by-member: same-`grouped_id` members are buffered ~1.5 s then forwarded in a **single** `forward_messages`, so Saved Messages keeps the album intact instead of N loose copies (see the unified-relay section).
5. A companion bot account (`BOT_TOKEN`) runs in the same process and provides a clickable inline-button menu in its private chat (buttons are bot-only — verified that user accounts' buttons are not rendered by official clients). Any message (or `/start`) to the bot shows the main menu — whose header carries a live status line (在途/待处理/待重试 + 今日成功个数与字节，打开即所见); callback presses execute the same service functions as the text commands and update the menu message in place. 2026-09-07 起 bot 对话里 `/` 命令同样执行（`bot_message_handler` 分发到 `handle_command`，未识别的回落主菜单），且启动时用 `SetBotCommandsRequest` 注册了原生命令面板（输入框点 "/" 弹出带说明的命令菜单，`bot.BOT_COMMANDS` 表）。The bot chat is auto-cleaned too (keeps the newest menu message).
6. All downloads go through a persistent queue (`download_queue.json`): media tasks are persisted before execution as message references (only `kind=media` — Saved Messages originals and whitelist-relayed copies), survive restarts, and failed tasks park in a retry list for manual re-execution instead of being lost.

All comments, log output, and user-facing Telegram replies are in Chinese. The startup banner identifies the program as "TG Userbot v2.10".

## Running

```bash
pip install telethon        # only dependency
python tg_userbot_final.py  # first run prompts for phone number + code interactively
```

一键启停（仓库根目录）：`./run.sh {start|stop|restart|status}`。管两个进程——userbot（`tg_userbot_final.py`）与 Chrome Agent（`python -m tg_userbot.chrome_agent`），**Chrome 专用实例刻意不管**（与 `/chrome_stop` 同款语义，重启 Agent 时直接复用它的 CDP）。`stop`/`restart` 走 SIGTERM 让两个进程自己优雅收尾（userbot 保存状态 + 断开 25 条 worker + 停清理任务；Agent 把 RUNNING 原样持久化），超时 `${STOP_TIMEOUT:-30}s` 才升级 SIGKILL 并明确告警。**代理**：优先环境变量 `TG_PROXY`，否则读 `tg_secrets.json` 的 `tg_proxy`（机器相关值只放那里，绝不写进公开脚本），两边都空时显著警告——直连在国内网络下连不上 Telegram，还会被误判成账号异常。进程探测用 `ps -eo pid=,command=` 全量匹配并**转小写比较**（本机 ps 显示 `.../MacOS/Python` 是大写 P，大小写敏感会漏判，写好第一版就踩了），且不依赖 `pgrep`（Termux 上不一定有）；Agent 走「PID 文件 + 命令行确属 chrome_agent」双路并集，比只认 PID 文件更能兜住「PID 文件丢了 / 有残留孤儿」——2026-09-10 实测撞到一个 22:54 起的 Agent 残留未退、与后起的并存了半小时（两个写者同时写 chrome_tasks.json），`status`/`start` 现在会就此告警。`start` 幂等（已在跑就跳过）；stdout/stderr 落 `runtime/userbot.out` 与 `runtime/chrome_agent.out`（主日志仍是 download.log / chrome_agent.log）。

- Before the first run, create `tg_secrets.json` by copying the template: `cp tg_secrets.example.json tg_secrets.json`, then fill in `api_id`/`api_hash` (my.telegram.org) and, for the button menu, `bot_token`/`bot_username` (@BotFather → /token). Optional machine-specific values go here too — the `cd2` segment (CloudDrive2 binary path + port + backup log dir, see the template) enables the menu's 【启动CD2】/【停止CD2】/【🗂 备份记录】 buttons. This file is gitignored and stays local.
- Platform defaults: Termux uses `SAVE_FOLDER=/storage/emulated/0/Download/Nagram`; macOS/desktop uses `SAVE_FOLDER=~/Downloads/Nagram`. Override with the `TG_SAVE_FOLDER` environment variable. The session is `~/tg_downloader` on both platforms (each device logs in independently; do not copy one device's `.session` file to another and run both simultaneously). Delete the `.session` file to force re-login.
- Optional proxy: set `TG_PROXY=socks5://127.0.0.1:7890` (or `socks4://`, `http://`, optionally with `user:pass@`) to route Telegram connections through a proxy — required when running on networks that block Telegram. Parsed by `parse_proxy()` into a Telethon proxy tuple passed to `TelegramClient`. Requires `python-socks` installed (`pip install 'python-socks[asyncio]'`); without it Telethon silently ignores the proxy argument. Unset → direct connection (unchanged Termux behavior). Hiddify's local mixed proxy port on this Mac is `127.0.0.1:12334`.
- Transport: `TG_CONNECTION=full|obfuscated` selects the Telethon connection class. **Default: `full` (TLS) — even when a proxy is set** (2026-09-05 change, originally `obfuscated`-when-proxy). `obfuscated` (MTProto obfuscated, no TLS, fully async) is now opt-in via `TG_CONNECTION=obfuscated` when anti-blocking/obfuscation is needed or a proxy is flaky. Why the default flipped — this is the **real download bottleneck** of the whole stack: the `obfuscated` transport wraps *every frame of every connection* in AES-CTR, and telethon 1.44's `AESModeCTR` (`crypto/aesctr.py`, which carries a `# TODO Use libssl if available`) only runs pure-Python pyaes in the single event-loop thread → a hard ~274 KB/s single-core ceiling (offline bench) regardless of socket count — live 10-worker `obfuscated` measured ~213 KB/s aggregate. `full` skips that per-frame cipher, so the 多 worker parallel connections actually parallelize (live 10-worker `full`: ~2.3–2.6 MB/s, ≈12×). `cryptg` would **not** fix this path (aesctr.py has no cryptg hook) — only dropping off `obfuscated` does. Cost of `full`: its TLS handshake is synchronous, so a stalled proxy node may defeat cancellation mid-connect. The `start_with_retry()` watchdog wraps `client.start()` in a 120s `wait_for` × 10 retries because Telethon's recv path has no read timeout and a stalled proxy node would otherwise hang forever. Steady-state (运行期) is guarded the same way: telethon 内建自动重连已关闭（`auto_reconnect=False`，见 config 的 `TELEGRAM_AUTO_RECONNECT`），掉线由 `app` 稳态守护（`_main_serve`/`_bot_keepalive`）带指数退避重连接管 —— 否则内建重连在「连上即再失败」（如代理持续回 HTTP 429）时会陷入无界递归风暴（实测叠上千层、卡死事件循环、进程退出）；`SIGINT`/`SIGTERM` 信号处理器把停止意图转成 `STOP_EVENT` 做优雅收尾（telethon 的 `run_until_disconnected` 会吞掉 KeyboardInterrupt，若不接管信号 Ctrl-C 会失效）。
- On macOS, the first run may trigger a system prompt asking to allow terminal access to the Downloads folder — allow it, or set `TG_SAVE_FOLDER` elsewhere.
- There is no build step or linter config. Unit tests (stdlib `unittest`, no extra deps) live in `tests/`: `test_whitelist.py` (whitelist), `test_menu.py` (button menu), `test_queue.py` (persistent queue), `test_display.py` (final-filename/source-link/album-caption display), `test_cleanup.py` (cleanup predicate incl. retained-media-copy protection), `test_platform_extract.py` (URL extraction regression), `test_commands.py` (text-command dispatch, `text` 遮蔽回归), and `test_workers.py` (多 worker 池簿记 + 借出前验活 + download_file 的 worker 路由分流 + 落盘前内容级去重拦截), `test_stats.py` (台账窗口统计/大小反解/文本渲染/命令匹配), `test_url_refresh.py` (直链过期防护：签名过期戳解码 + 开下前刷新 + 403 短路降级解析 bot; url 任务内容级去重拦截/落盘); `test_dedup.py` (重复媒体去重：tg:/dyc:/f:/c: 四类键构造、append-only 索引 load/裁剪、多键 should_skip（列表任一命中即拦 + 旧单字段兼容）、内容级 content_seen/多键 remember、开关持久化、enqueue_media 入队接线 dedup_keys), `test_caption_filter.py` (Caption 命名清洗：规则解析/四类前缀只切第一个冒号、默认规则的真实 Caption 验收输出、field 剥标签留值与五条防误删、中英文冒号/换行字段/值含空格/字段乱序、空白归一不破 #标签、非法规则跳过、清洗不截断、配置持久化与增删清空恢复、/caption_filter 命令解析、命名链路接入（清洗在 sanitize 之前 + 只清一次 + 抖音标题不清）), `test_retry_sweeper.py` (自动重放后台扫描接线：每跳必调 replay_due、单次扫描抛错不打死亡循环、真取消原样上抛。退避/上限/空闲 worker 预算语义在 test_queue.py::AutoReplayDueTest), `test_reporter.py` (Runtime Reporter：纯函数格式化/面板文本/长度预算、面板创建与原地编辑、四类 Telegram 异常、事件游标增量读、错误去重、异常隔离、统计缓存、汇报目标解析、**网络层取消不杀循环 / 僵死请求不冻循环 / 面板 id 不丢 / supervisor 意外重启与停服不重启**), `test_netio.py` (网络调用收口：成功回值、网络层取消→None 且不打断调用方、超时→None 并回收子任务、普通异常→None、停服真取消原样上抛且不留孤儿子任务), `test_notify.py` (通知出口：bot 账号→控制面板优先、bot 缺席/掉线/发送失败三条回落收藏夹、网络层取消不冒给调用方、全败返 False), `test_runtime_db.py` (Runtime DB：schema/迁移幂等、WAL 与 PRAGMA 如实反映、checkpoint、**UNIQUE 对 NULL 收藏夹目标也生效**、任务事务原子性（checkpoint 写失败则整批回滚）、claim/complete/retry/fail/cancel、租约恢复、SQLITE_BUSY 有限重试、事件外键、统计), `test_listener_worker.py` (Worker：错误分类与退避、FloodWait 采信服务端值并暂停整个 Worker、临时/永久错误、重试耗尽、租约恢复、节流、优雅停机、强引用、单任务失败不退出), `test_listener.py` (Scanner：标签边界、目标身份、相册分桶、规则校验、配置持久化与重载保护、**规格书 §27 测试 A–G**、队列上限背压、相册边界补齐、旧状态迁移、/listen 命令与视图), 三者的网络与 DB 全部注入替身（不联网）; run from the repo root with `.venv/bin/python -m unittest discover -s tests -p "test_*.py" -v`. 每个测试模块都会在 import `tg_userbot` 之前把 `TG_SAVE_FOLDER` 指到自己的临时目录（import 包本身会创建 `SAVE_FOLDER` 并配置一次 logger），并 `atexit` 注册退出时回收——否则每跑一次 suite 就在 TMPDIR 里留 20+ 个目录（实测堆到过 1700 多个）。测试不需要 `tg_secrets.json`（缺失只是取空默认值）。 A local `.venv` is used for Mac development: `python3 -m venv .venv && .venv/bin/pip install telethon 'python-socks[asyncio]'`.
- The bot account logs in with its own session file (`~/tg_downloader_bot.session`, separate from the user session). Its login is `start_with_retry(bot_client, bot_token=BOT_TOKEN)`; a failure logs an error but does not stop the userbot (menu just stays unavailable).

## Configuration

Non-secret settings live in `tg_userbot/config.py` (platform detection via `is_termux()`/`IS_TERMUX`, `SESSION_NAME`, `SAVE_FOLDER`, `LOG_FILE`, `DOWNLOAD_RETRIES`, Douyin/Instagram URL regexes + parse-bot username/labels (`PLATFORM_LINKS`), auto-clean settings (`CLEAN_MESSAGE_AGE_MINUTES`, `BOT_CHAT_KEEP_NOTIFICATIONS`=控制面板额外保留的最近通知条数), 通知/汇报的网络超时与守护 (`NOTIFY_TIMEOUT_SECONDS`, `REPORT_NET_TIMEOUT_SECONDS`, `REPORT_RESTART_DELAY_SECONDS`), `BOT_SESSION_NAME`, the runtime JSON filenames, and the `AdjustableSemaphore` class). Settings should only be changed there. Platform branches must keep the Termux defaults intact. **All non-media runtime files live together in `SAVE_FOLDER/runtime/` (`RUNTIME_DIR`)**: the 7 runtime JSONs `clear_time.json`/`thread_config.json`/`whitelist_config.json`/`download_queue.json`/`dedup_config.json`/`caption_filter.json`/`listen.json`, plus `tg_userbot.db`（标签监听的业务状态，SQLite；旧的 `listen_state.json` 已退役，首次启动自动迁入并改名 `.migrated`）, plus `download.log` (按天轮转)、`chrome_agent.log` (Chrome Agent 进程独占，**刻意与 download.log 分开**——两个进程各持一个 TimedRotatingFileHandler 写同一文件时午夜会互相覆盖归档，见 issues/001)、`download_history.txt`、`dedup_index.txt`、`task_events.jsonl` (任务生命周期事件，台账按 task_id 重建的数据源)、`cd2_launch.log`. 旧版本散在 `SAVE_FOLDER` 根目录下的这 7 个同名文件在 import 期被 `config._migrate_runtime_files()` 自动迁入（幂等：仅当 runtime/ 下尚无同名文件才搬，绝不覆盖新版、绝不误伤其它文件），所以升级后无需手动搬。媒体仍存 `SAVE_FOLDER/<来源>/`（不受影响）。

**Real credentials are NOT hardcoded.** `api_id`/`api_hash`/`bot_token`/`bot_username` are read at import from `tg_secrets.json` in the **repo root** — computed as `REPO_ROOT` in `config.py` from the package's `__file__` (deliberately *not* the `tg_userbot/` package dir, which is where a naive `__file__`-relative lookup would land after the refactor); override the path with `TG_SECRETS_FILE`. That file is gitignored and never committed — copy the committed template `tg_secrets.example.json` to `tg_secrets.json` and fill in real values (api_id/api_hash from my.telegram.org, bot_token/@bot from @BotFather). A missing `api_id`/`api_hash` aborts startup with a clear message; a missing `bot_token` only disables the button menu (downloading still works). Never commit real tokens to the repo — the public remote contains no secrets. Machine-specific paths that reveal the local username also belong in this file, never in code — e.g. the optional `cd2` segment (binary path + port + log dir) that powers the menu's 【启动CD2】/【停止CD2】/【🗂 备份记录】 buttons.

The bot is operated at runtime through Telegram commands sent to Saved Messages (not the shell): `/status`, `/folder`, `/logpath`, `/done [n] [keyword]` (download history: last n lines, default 10; fuzzy case-insensitive substring match on keyword, e.g. `/done 20 古风`), `/progress` (live progress of in-flight downloads), `/thread [n]` (show/set parallel-download worker connections, 1–25, persisted, applied live), `/dedup [on|off]` (重复媒体去重：无参看状态与索引条数，off/on 切换并持久化), `/wl [list]` (list the download whitelist), `/wl add <id|@username>` (add a chat; alternatively reply to a forwarded message from that chat with `/wl add` — resolves `fwd_from`), `/wl del <id|list-index>` (remove), `/queue` (list the active download queue), `/queue del <n>` (remove a task; 任务正在执行时先真正取消在途下载——task.cancel 沿真取消链路清 .download 半成品、归还 worker——再移除记录并通知「🛑 已取消下载并移除」；排队中的任务照旧只移除。删除本身落日志「🗑 手动移除队列任务（在途已取消/未在途）：label」——这是台账「移除」桶的唯一数据源（2026-09-08 前 /queue del 完全无痕，当天台账曾因此差 4 条无法勾稽）。任务句柄由 `queue._RUNNING_TASKS` 按 record id 登记，`queue.queue_del_task` 是命令与菜单共用的删除/取消入口), `/retry` (list the failed-task list), `/retry <n>` (manually re-execute a failed task), `/retry del <n>` (remove from the retry list), `/stats [n]` (台账/对账：默认今日、`/stats 3` 最近 3 天；窗口内有任务事件按 task_id 严格重建，无事件回落关键词估算), `/find <关键字>` (媒体下落查询：队列+近7天日志+全部历史里完整字段子串搜索，每条命中给终态), `/caption_filter [add <规则>|del <序号>|clear|reset|test <文本>]` (Caption 命名清洗规则：无参看规则列表，add/del/clear/reset 改规则并持久化，test 用**真正的** clean_caption 试跑一段文本；规则改动实时生效、重启仍在), `/listen [on|off|scan]` / `/listen interval <分钟>` / `/listen del <序号>` / `/listen add <聊天> <标签> [目标,目标] [on|off]` (标签监听：主动扫描独立于下载白名单的监听来源，命中标签后转发目标并按需下载；见「标签监听」段), `/help`, `/clean` (delete stray `.download` temp files), `/clearmsg` (manually delete program messages), `/setcleartime 1m|off` (change auto-clean interval). `/chrome_start` / `/chrome_stop` / `/chrome_status` / `/chrome_tasks` / `/chrome_cancel <序号|task_id>` / `/chrome <URL>` (Chrome Agent：远程启停专用 Chrome 下载器、查状态、列可取消任务、取消单条任务、提交 URL 下载任务；owner-only，见 Chrome Agent 段). Commands are defined in `handle_command`; the handler returns `False` for unknown `/` commands so they can fall through to download logic if they carry media. `/wl`, `/queue`, and `/retry` commands and replies are covered by the auto-clean whitelist (`is_wl_command`/`is_queue_command`/`is_retry_command` + reply prefixes in `CLEAN_NOTIFICATION_PREFIXES`).

Completed downloads are appended as one line per record to `DOWNLOAD_HISTORY_FILE` (`download_history.txt` inside `SAVE_FOLDER/runtime/`), format: `时间 | 类型(普通) | 文件名 | 大小 | 来源`. Since the unified-chain refactor all downloads (incl. douyin/IG video copies relayed into Saved Messages) go through `download_file`, so every record is type `普通` — the old platform type `抖音` and the `download_bot_video` writer are gone. Writes happen after the atomic `os.replace` in `download_file` (download.py); each calls `history.append_history`, a **lockless synchronous append** — single-line short writes in `"a"` mode under the single-threaded event loop are atomic, so no `DOWNLOAD_LOCK` exists (the real concurrency primitives are `DOWNLOAD_SEMAPHORE`, `QUEUE_LOCK`, `CLEAR_TIME_CHANGED`, all in `state.py`). `/done` reads the last n lines via `history.get_history_lines` and truncates the reply to fit Telegram's 4096-char limit. `/done` commands and replies are included in the auto-clean whitelist (`history.is_done_command` + reply prefixes in `CLEAN_NOTIFICATION_PREFIXES`).

## Architecture

**Module index.** All code lives in the `tg_userbot/` package. Importing any submodule bootstraps the package exactly once (`__init__.py` → `config.py` → at config's end: `os.makedirs(SAVE_FOLDER)` + `os.makedirs(RUNTIME_DIR)` + `config._migrate_runtime_files()` (旧版根目录运行时文件迁入 runtime/) + `log.configure(LOG_FILE, LOG_RETENTION_DAYS)`), so every entry point (thin shell, tests) gets the same init. Roles:

- `app.py` — entry wiring: `create_client`/`create_bot_client`, `start_with_retry`, `new_message_handler` (the single event entry), `main()`. The **only** module that constructs loop-bound primitives; `main()` assigns the initial real value to every `state.*` global (runtime adjustments like `/thread n` still write `state.DOWNLOAD_CONCURRENCY`). Login 后它还调用 `workers.spawn_pool()` 建下载 worker 池（客户端与空闲队列都在 main 里建，事件循环规则）。稳态用「服务任务 + `STOP_EVENT`」：`main()` 尾部创建 `_main_serve()`（主客户端 `run_until_disconnected` + 掉线自动重连）、`_bot_keepalive()`（bot 探活重连）与 `_retry_sweeper()`（retry 榜到期自动重放）三个后台任务后挂起等 `state.STOP_EVENT`（同样在 `main()` 内创建）；`SIGINT`/`SIGTERM` 处理器设 `STOP_EVENT` → 取消服务任务、断开下载 worker、停自动清理任务后退出。
- `config.py` — read-only constants, secret loading, platform detection, URL regexes, parse-bot username/labels (`PLATFORM_LINKS`), `AdjustableSemaphore` class. Settings are changed here.
- `log.py` — shared `logger` + `configure()` (idempotent).
- `state.py` — the runtime-mutable globals (`client`, `bot_client`, `MY_ID`, `QUEUE`, `QUEUE_LOCK`, `EXECUTING`, `DOWNLOAD_SEMAPHORE`, `DOWNLOAD_CONCURRENCY`, `DOWNLOAD_WORKERS`/`DOWNLOAD_WORKER_QUEUE`/`DOWNLOAD_WORKER_TARGET` (worker pool), `PROCESSING_DOUYIN_IDS`, `ACTIVE_DOWNLOADS`, `CLEAR_INTERVAL_SECONDS`, `CLEAR_TIME_CHANGED`, `WHITELIST_CHATS`, `_CD2_PROC`, …) as `None`/`{}` placeholders until `main()` fills them.
- Pure leaves `naming.py` (filename/caption/extension math, album-caption sibling text picker `pick_group_caption_text`), `sources.py` (chat/forward-source resolution, message links), `history.py` (download-history append/read, `is_done_command`), `netio.py` (`shielded(proc, timeout, what)`——**网络调用收口**：请求放进子任务、结局经 `result()` 读取，从结构上区分「网络层取消（telethon 断线对 pending future 调 cancel()）→ 返回 None、本轮跳过」与「调用方被真取消（停服）→ 原样上抛」；超时到点取消子任务，僵死连接不会把调用方的循环永久冻死。cleanup / reporter 的每条网络请求都经它，见下方「网络层取消」段).
- Feature modules `whitelist.py` (`/wl`), `thread.py` (`/thread`), `download.py` (`download_file` + in-flight progress registry; `download_file` 内部借 worker 传字节——多 worker 路由的唯一切入点，`queue.py` 不改；另有 `download_url_media`（本地解析链的 HTTP 直链下载分支）), `notify.py` (程序通知统一出口 `notify_user(text)`：bot 账号 → 控制面板对话，cli 缺席/失败回落收藏夹；见下方「程序通知的去向」段), `workers.py` (多 worker 下载池：MemorySession 拷授权、spawn/扩缩容/借还/退出断开), `resolver.py` (抖音链接本地解析：f2 短链展开 + 作品详情 + 最高码率直链挑选，任何失败返回 None 降级 bot), `dedup.py` (重复媒体去重：tg:<file_unique_id> / dyc:<aweme_id> 判重键、append-only 判重索引 dedup_index.txt 的 remember/load/裁剪、入队前两级判重 should_skip、/dedup 开关持久化), `caption_filter.py` (Caption 命名清洗：exact/contains/regex/field 四类规则的解析与执行——field 剥标签留值、字段名后紧跟括号也算字段起点、非字段处的自然语言不误删；规则持久化 caption_filter.json 与 /caption_filter、菜单共用的服务函数；纯函数 clean_caption 不碰 I/O), `platform.py` (Douyin/Instagram URL extraction + `relay_platform_links` — 桌面端抖音链接先本地解析入 url 队列任务，失败/未启用才 relay 解析 bot；Instagram 始终 relay), `queue.py` (persistent queue pure functions + `enqueue_and_start`/`execute_queued_task`/`recover_queue_tasks`/`replay_due`(到期自动重放)/`retry_all`; kind=media/url 两类), `cleanup.py` (auto-clean loop, `clean_temp_files`, bot-chat cleanup), `text.py` (shared `status_text`/`progress_text`/`done_reply_text`/`wl_list_text`), `menu.py` (menu data codec + button builders), `cd2.py` (CD2 start/stop + backup records), `stats.py` (台账/对账：任务事件层 emit_event/load_events（runtime/task_events.jsonl，JSONL append-only）+ 按 task_id 重建窗口统计 rebuild_stats + 渲染；窗口内无事件回落 download.log/download_history.txt 关键词口径), `finder.py` (媒体下落查询 `/find`：当前队列 + 近 7 天日志（[T=] 聚合）+ 全部历史，完整字段子串搜索，零新增持久化), `listener.py` (标签监听 **Scanner**：按周期主动扫描独立的监听来源聊天，命中配置的标签后**只把结果落成 SQLite 任务**，转发由 worker 受控执行——**与下载白名单完全独立**，见下方「标签监听」段), `listener_worker.py` (标签监听 **Worker**：常驻受控消费 listener_tasks——claim + 租约 → forward → SUCCESS/退避重试/FAILED，FloodWait 采信服务端值并暂停整个 Worker，download=1 时转发进收藏夹后调现有 enqueue_media), `runtime_db.py` (Runtime DB：标签监听业务状态的 SQLite 层，**全项目唯一的 SQL 出口**——连接/schema/迁移/checkpoint/任务 CRUD/事件/统计；import 期不建连接), `reporter.py` (Runtime Reporter：**只读观察者**，把运行状态汇报到 Telegram——Status Panel 单条消息原地编辑 + 重要事件独立通知；数据源全部是既有 state/事件流，不建第二套状态、不改任何业务逻辑；主循环由 `app._reporter_supervisor` 守护、每条网络请求经 `netio.shielded`，见下方「网络层取消」段). `chrome_agent.py` (Chrome Agent V1：CDP 驱动专用 Chrome 实例下载——任务状态机/持久化/恢复/重试 + CDP 客户端与事件归因，`python -m tg_userbot.chrome_agent` 为进程入口), `chrome_client.py` (Chrome Agent 的 User Bot 侧：/chrome* 命令、Agent 进程管理（PID 文件+存活检测）、chrome_requests.json 用户映射、结果通知轮询).
- Thin dispatchers `commands.py` (`handle_command`) and `bot.py` (`handle_menu_action`, `bot_message_handler`, `bot_callback_handler`).

Dependency rule: leaves (`config`/`log`/`state`/`naming`/`sources`/`history`) may be function-imported (`from .config import X`); runtime cluster modules import each other only as `from . import X` and call `X.func()` at call time. (The former sole module cycle `queue ↔ platform` was dissolved by the unified-chain refactor: `platform` no longer imports `queue`, and `queue` no longer imports `platform` — the call-time rule now just protects forward references generally.) Functions under test-patch (`save_whitelist`, `save_queue`, `get_forward_source`, `execute_queued_task`) keep bare-name calls inside their own module so `mock.patch.object(module, name)` stays visible.

**Event loop rule (critical).** Only `app.main()` — running inside `asyncio.run` — constructs loop-bound primitives: the `TelegramClient`s (`app.create_client`/`create_bot_client` plus the download-worker `TelegramClient`s and their idle `asyncio.Queue` from `workers.spawn_pool`/`sync_pool_to_target`, which are likewise only invoked from `app.main()`), the `asyncio.Lock` (`QUEUE_LOCK`), `asyncio.Event` (`CLEAR_TIME_CHANGED`), and the `AdjustableSemaphore` (`DOWNLOAD_SEMAPHORE`). Every one of these lives in `state.py` as a `None`/empty placeholder until `main()` assigns a real value, so **no module ever constructs one at import time**. On Python ≤3.9, asyncio primitives bind to the event loop that exists at construction time; a module-level client would bind to a stale loop and `asyncio.run(main())` creates a new one, causing connections to establish but every request to hang forever with no error ("got Future attached to a different loop"). The Android/Termux install was unaffected only because its Python is ≥3.10, where primitives bind lazily. Runtime globals are read/written as `state.X` (never `from .state import X`); read-only constants may be top-level `from .config import X`. Handlers are registered on the client inside `main()` (`client.add_event_handler(new_message_handler, events.NewMessage())`) for the same reason.

**Single event entry point.** One `events.NewMessage()` handler passes the message through `classify_message_chat(chat_id, MY_ID, WHITELIST_CHATS)`: `("me", None)` for Saved Messages, `("chat", title)` for whitelisted chats, `(None, None)` otherwise (ignored). It then dispatches in this order, each branch returning early:

1. `/` commands → `handle_command` (only when the message came from `"me"` — whitelisted chats can never control the bot)
2. Douyin/Instagram URLs in message text or link entities → `platform.relay_platform_links` → send the link text to the parse bot (only for `"me"`)
3. Downloadable media → `"me"`: `enqueue_media` the original message; whitelisted chat: `relay_chat_media` forwards a copy to Saved Messages then `enqueue_media` that copy — single media immediately, album members batched into one grouped forward (forward failure falls back to downloading the original directly)

Media relay/enqueue and link relay are fired with `asyncio.create_task`, so the handler never blocks on network I/O.

**Download whitelist (unified relay).** `WHITELIST_CHATS` (`{chat_id: title}`, chat ids are Telethon "marked" ids — negative for channels — obtained via `telethon.utils.get_peer_id`, which is what makes membership checks match `event.chat_id`) is loaded at startup from `whitelist_config.json` via `load_whitelist()` and mutated at runtime by `/wl` commands (persisted via `save_whitelist()`). A whitelisted chat's media is **relayed** rather than directly downloaded: `relay_chat_media` (app.py) forwards the message to Saved Messages (`_forward_to_me` → bare `message.forward_to("me")`, deliberately **not** wrapped in `wait_for`), keeps the copy, and enqueues it (`chat_id=MY_ID`). A big album burst (~20 members forwarding at once) trips Telegram's forward FloodWait; Telethon auto-sleeps below its threshold and `_forward_to_me` catches the above-threshold `FloodWaitError` to sleep (≤120s) and retry — a `wait_for` cap would cancel that legal wait into a timeout and degrade the member to direct-download, splitting one album across two source folders (live-observed: several of the same album's members landed under `1仓鼠_3D区/` while the rest went to `祂录（3D区）/`). A real forward is used rather than a self-authored `send_file` copy so the Saved Messages copy keeps the system **「转发自 <来源>」 header** — the user reads it to tell which channel the content came from and where it will be saved; the folder is then resolved from the copy's own `fwd_from` at download time (`resolve_download_source()` → `get_forward_source()`), so a re-forwarded album still lands under its true origin (e.g. `祂录（3D区）/`) — no `source_override` needed. Known cost (user-approved): a source carrying inline buttons keeps them on the forwarded copy, since Telegram forbids editing forwarded messages. A forwarded copy also can't gain a caption, so album-caption inheritance is handled for **naming** instead of for the message: for a grouped member with no own text, `_maybe_album_caption` → `_fetch_group_caption` (a ±10-id sibling window in the source chat → `pick_group_caption_text`) reads the group caption once at relay time (cached per `grouped_id` in `_ALBUM_CAPTIONS` so the burst's other caption-less members don't each fire another `get_messages`); it's persisted on the queue record as `album_caption`, and both the enqueue-time display name and the download (`download_file(..., caption_override=record["album_caption"])`, shared by `queue._run_queued_task`) use it — `compute_final_filename(message, caption=…)` takes the caption override (a pure leaf change). Saved Messages originals get the same treatment via `_enqueue_me`. `enqueue_media` still accepts an explicit `source_link` (the original source's clickable link) for the queue display. If the forward fails (source forbids forwarding etc.), `relay_chat_media` catches the error and falls back to `enqueue_media` on the original message (source_override = chat title). Whitelist members still never get commands or platform-link parsing. **Album members are forwarded as one group, not N loose copies.** A Telegram album reaches the whitelist chat as N independent media events sharing one `grouped_id`; forwarding each on arrival would leave N separate messages in Saved Messages. Since 2026-09 `relay_chat_media` dispatches on that: no `grouped_id` → `_relay_single` (immediate single forward, the behavior above); has `grouped_id` → album coordinator `_relay_album_member`, which registers the member into per-`(chat_id, grouped_id)` state in `_ALBUM_RELAY` and has the first member start `_relay_album_group`. That task sleeps `_ALBUM_DEBOUNCE_SECONDS` (~1.5 s) so the burst's other member events land, then re-reads the **authoritative full member list** from the source chat (`_fetch_album_members`, a ±10-id window around the seen span filtered by `grouped_id` + `is_downloadable` — catching any member whose event didn't fire and locating the caption-holder) and forwards the whole group in **one** `client.forward_messages("me", members, from_peer=chat)` (`_forward_album_to_me`, same FloodWait retry as `_forward_to_me`) — Telegram re-groups the copies into a single album in Saved Messages. Each returned copy is enqueued (`chat_id=MY_ID`) sharing the group's source link; a copy carrying its own text keeps it, caption-less copies get the group caption as `album_caption` (naming inheritance unchanged). The coordinator keeps a `done` tombstone through `_ALBUM_SETTLE_SECONDS` (~5 s) then pops the entry, so a straggler member event is ignored rather than double-forwarding the group. Single media is unaffected (no delay). On failure the coordinator degrades to `_fallback_relay_each` (per-member `_relay_single`, the pre-batch behavior) so nothing is lost — batch forward error or empty source re-read both land there. Whether Telegram keeps an oversized album (>10 members) as one group or splits it across groups when forwarding into a user chat is server behavior and was still pending live verification on 2026-09-05. Pure helpers (`classify_message_chat`, `parse_wl_command`, `is_wl_command`, `resolve_wl_del_key`, `entity_display_name`, whitelist load/save) are unit-tested in `test_whitelist.py`; the Telegram-facing `/wl` flow is verified by live testing.

**Button menu (bot account).** Buttons of any kind are bot-account-only — empirically verified (official clients render no keyboard at all on user-account messages). So a second client `bot_client` (created in `main()`, `create_bot_client()`, session `~/tg_downloader_bot.session`) logs in with `BOT_TOKEN` and hosts the menu. Callback payloads are `m:<action>[:<arg>]` (`encode_menu_data`/`parse_menu_data`, ≤64 bytes; `m:thread:5`, `m:wl_del:-100123`, …). `bot_message_handler` answers any owner message with the main menu, or a confirm-add flow when the owner forwards a message (forward source → `add_to_whitelist`); non-forwarded `/`-prefixed texts are dispatched to `commands.handle_command` first (so the registered command palette works in the bot chat; unknown commands incl. `/start` fall through to the menu); `bot_callback_handler` answers the query (`event.answer()`), dispatches via `handle_menu_action`, and edits the same message with the result. Both handlers check `event.chat_id == MY_ID` (owner-only). Text commands and menu presses are both thin dispatchers — `commands.handle_command` and `bot.handle_menu_action` — over the same shared module functions: `text.status_text`/`progress_text`/`done_reply_text`/`wl_list_text`, `thread.apply_thread_limit`, `cleanup.clean_temp_files`, `whitelist.add_to_whitelist`/`del_from_whitelist`. Menu-only actions are 【🛡 去重】 (`dedup` 状态视图/`dedup_toggle` 开关，视图按钮组 `dedup_menu_buttons`；主菜单位于「🧹 清理」下一行), 【启动CD2】/【停止CD2】 (`cd2`/`cd2_stop`), 【🗂 备份记录】 (`bak`), 【📊 台账】 (`stats`, `m:stats:<n>` 切换 今日/3 天/7 天窗口), and 【🌐 Chrome 任务】 (`chrome_tasks`＝`/chrome_tasks` 的同一份正文，视图按钮组 `chrome_menu_buttons`：**每条可取消任务一个 🛑 按钮**、载荷 `m:chrome_cancel:<task_id>`，外加 ▶️ 启动 Agent / ⏹ 停止 Agent / 🔄 刷新 / ℹ️ Agent 状态 / 🏠 返回——🛑 直接带 task_id 而非序号，天然绕开 `/chrome_cancel` 序号形态的漂移坑，用户也不用去数序号；启停与状态此前只在 Telegram 命令面板里、按钮菜单够不着，2026-09-10 补齐)。队列/待重试/进度视图尾部有「🔄 刷新」按钮原地重渲染（省去 返回主菜单→再进 的 4 个代理往返；内容无变化时 MessageNotModifiedError 已静默处理）: the CD2 pair probe whether the local CloudDrive2 process is up on its management port and start or stop it via the configured standalone binary (start: detach + poll port; stop: match its PIDs by exact binary path and SIGTERM/SIGKILL); 备份记录 reads CD2's `backup.<日期>.log` to list media that was backed up and deleted locally. Config from the `cd2` segment of `tg_secrets.json`, never hardcoded — see the "磁盘回收" section. Bot-chat auto-clean (`cleanup_bot_chat_once`, same loop/interval as Saved Messages cleanup): `plan_bot_chat_cleanup` deletes messages older than `CLEAN_MESSAGE_AGE_MINUTES` but keeps three things — the newest message with buttons (the live menu), the newest Runtime Reporter status panel (it's refreshed in place; deleting it makes the next round rebuild it, i.e. one more panel every two minutes forever), and **the newest `BOT_CHAT_KEEP_NOTIFICATIONS`(=20) other messages** (2026-09-11 起通知都发到这个对话，删光了面板就没法回溯；按**条数**而非时长保留，空闲多久都不会攒出一屏旧消息，总量 ≈22 条封顶). Menu-related pure functions are unit-tested in `test_menu.py`; the Telegram-facing menu flow is verified by live testing.

**程序通知的去向（2026-09-11）。** 主动通知（`📥 开始下载`/`✅ 下载完成`/`❌ 文件下载失败`/`⏭️ 内容重复已拦截`/`🛠 本地解析成功`/Chrome 结果/汇报事件…）一律经 `notify.notify_user(text)` 发到 **bot 控制面板对话**，由 **bot 账号**发出；**收藏夹只留媒体**（转发副本 + 用户自己发的命令及其回复——命令回复仍回到发出命令的那个对话）。理由：收藏夹的职责是媒体容器（转发副本的「转发自」头、`fwd_from` 决定落盘目录、手工转发评论靠那里的纯文本认标注），通知混在里面既是噪声又干扰标注识别。由 bot 账号（而不是主账号发到同一对话）是刻意的：bot 自己的出站消息不会回流成更新，因此不会被 `bot_message_handler` 当成 owner 指令、每发一条通知就回一次主菜单（实测刷屏）。bot 未配置/未连接/发送失败 → 自动回落主账号发收藏夹，通知绝不丢。发送经 `netio.shielded` 收口——通知是下载收尾路径上的一步，它自己因断线冒出的 `CancelledError` 会把正在下载的任务误判成「被真取消」而留在队列里，通知不该有这种权力。**例外**：Status Panel 仍由主账号发并原地编辑（bot 账号对消息有 48 小时编辑时限，长期面板会被卡住）；`bot_message_handler` 开头即以 `REPORT_STATUS_PREFIX` 忽略面板正文，避免它被当成 owner 指令或落进 cookie/查询输入窗口。

**网络层取消（telethon 断线 cancel pending future）——本项目最贵的一个坑，已咬过三次。** telethon 断线会对 pending 请求 future 调 `cancel()`；py3.8+ 的 `CancelledError` 是 `BaseException`，会绕开 `except Exception` 一路冒到异步循环外，把循环当停服信号打死、且**无任何日志**。三次事故：① 清理循环被杀死 → 收藏夹通知数小时无人清理；② 下载任务静默消失（队列有任务、`/progress` 却空）；③ **2026-09-11 汇报循环被杀 → 07:08 起状态面板与事件通知全部停摆数小时，直到用户发现「不再主动汇总数据」**。处置分三层：`download`/`queue` 在子任务的 `.result()` 处把网络取消转成可重试的 `ConnectionError`；`netio.shielded` 是所有其余网络调用（cleanup / reporter / notify）的统一收口——网络层取消 → 返回 None（本轮跳过、下轮再试），停服真取消 → 原样上抛，并顺带用超时兜住「无读超时的僵死连接」（`REPORT_NET_TIMEOUT_SECONDS`/`NOTIFY_TIMEOUT_SECONDS`）；最后是 `app._reporter_supervisor` 这道守护网——`reporter_task` 若仍以取消/异常收场且非停服，记 ERROR 并以 `REPORT_RESTART_DELAY_SECONDS` 节流重启同一条实例（保住 `status_message_id`，面板继续原地编辑）。判据是 `state.STOP_EVENT`：只有 main() 收到停止信号才置位。

**台账/对账（stats，2026-09-07 起版；2026-09-09 升级为 task_id 生命周期统计）。** 一个逻辑下载任务围绕**同一个 task_id（= 队列记录 id）**记完整生命周期：`RECEIVED → QUEUED → RUNNING → (RETRY → RUNNING)* → SUCCESS / FAILED / CANCELLED / REMOVED / DEDUP_HIT`；`DEDUP_SKIPPED` 是「收到但未产生任务」的输入侧事件（无 task_id）。事件落 `runtime/task_events.jsonl`（JSONL，append-only 单行追加，写失败仅告警绝不影响下载；启动 `stats.trim_event_file()` 裁剪到 `TASK_EVENTS_MAX_EVENTS`=3 万条保尾部）。

**发点（全部一行 emit，不动任何控制流）**：`RECEIVED+QUEUED` 在 `queue.enqueue_and_start`（媒体与 url 任务唯一的入队咽喉——白名单「源消息→转发副本」也只在这里入队一次，天然保证一个逻辑任务一个 task_id；RECEIVED 仅 kind=media，src=me/wl 按 chat_id==MY_ID 拆分）；`RUNNING` 在 `execute_queued_task` 开头；`RETRY`+`FAILED` 在失败转 retry/retry 内再失败（attempts 随记录累计）；`REMOVED` 在 `_run_queued_task` 的原消息已删除（why=source_deleted）与未知类型（why=unknown_kind）分支、以及 `queue_del_task` 未在途删除（why=manual）；`CANCELLED` 在 `queue_del_task` 取消在途；`SUCCESS(bytes=精确字节数)` 由下载层在 os.replace 后发（`download_file` 新增可选参数 `task_id`，唯一调用方 `_run_queued_task` 传入；`download_url_media` 用 record id）——精确 bytes 不再靠 format_size 反解；`DEDUP_HIT` 在内容级拦截点（没落盘不算成功，独立终态）；`DEDUP_SKIPPED` 在 `app.enqueue_media`/`platform` 抖音判重命中。**retry 重放复用同一 record** → 同一 task_id，统计上不是新任务；**真取消（进程退出）不发终态** → 任务在事件流保持「无终态」，重启 recover 重跑后补上，对账天然平衡。

**统计重建**（`stats.rebuild_stats`，纯函数可注入）：窗口任务集 = 窗口内有任何带 id 事件的 task；每个任务按**窗口内最后一个终态**归类 成功/最终失败/移除(REMOVED+CANCELLED，拆 手动/原消息删除/取消)/拦截/进行中；重试次数 = 窗口内 RETRY 事件数（失败尝试单独计次，不混进任务数）；成功容量 = 窗口内 SUCCESS 事件的精确 bytes 累加。**对账恒等式严格成立**：`窗口任务集 = 成功 + 最终失败 + 移除 + 拦截 + 进行中`（按构造是任务集的分区，✓ 恒打；跨日任务在两侧窗口各按当时终态计，事件流可完整解释）。**兼容**：窗口内无事件（功能上线前的老日子）→ 回落旧关键词口径（`collect_stats`：download.log 子串计数 + history 行数 + parse_size 反解）并在尾部注明「关键词估算」；在途/待处理/待重试三行仍是 `state` 实时快照。渲染分节：📥 输入事件 / 📦 下载任务 / 🔄 执行情况 / ⏳ 当前存量 / 💾 成功容量 / 🧮 对账。入口不变：`/stats [n]` 与主菜单【📊 台账】（`m:stats:<n>` 切窗口），前缀「📊 台账」在自动清理白名单。单测在 `test_stats.py`（事件层 emit/load/裁剪、rebuild 八场景：retry3 成功/多次失败终失败/移除三源/去重不产任务/内容拦截/跨日/bytes 精确/无终态进行中、新渲染与 legacy 回落）、`test_queue.py`（队列层发点集成：入队/重试同 id/取消删除/原消息删除）、`test_workers.py`+`test_url_refresh.py`（下载层 SUCCESS 精确 bytes 与 DEDUP_HIT）。

**媒体下落查询（finder，2026-09-08）。** `/find <关键字>`（≥2 字符）+ 主菜单【🔍 查询】按钮（按钮复用 cookie 的输入窗口模式：按下→提示→窗口内下一条文本即关键字，`FIND_INPUT_WINDOW_SECONDS`=120s、`state.FIND_INPUT_UNTIL`，/ 开头取消）。起因：/retry 视图长名截尾 48 字符把名字头部的期数词截掉，「bl44专属」三条明明在待重试列表里却翻不到。查询搜**完整字段**的忽略大小写子串，三个数据源按可信度取单一真相：① 当前队列 tasks/retry（⏳/🔁，freshest）；② download.log 近 7 天（复用 stats 的窗口读行）——无 trace 行独立成事件（📨 收到/⏭️ 去重跳过/🗑 已移除/❌ 原消息已删除），有 trace 的按 [T=] 收**全组**（终局行往往不含关键字）再按组内任一行命中过滤，终态 🔁 失败/❓ 中断；组内任一行 ✅ 下载完成 → 不单列（历史已有），组 id8 与队列记录 id 相同 → 不单列（队列已代表）；③ download_history.txt 全量倒序（✅ 已下载）。长名修剪保两头：`_trim(n=64, keep_tail=28)`——caption 在头部（关键字所在）、真文件名在尾部（同组条目的区分点），只保一头会让三条命中看起来一模一样。回复前缀「🔍 查询」+ `/find` 命令进自动清理白名单，命令面板注册 find。纯函数（路径/日期/队列注入）单测在 tests/test_finder.py；菜单按钮在 test_menu.py；接线在 test_commands.py/test_cleanup.py。

**标签监听（listener / listener_worker / runtime_db，2026-09-11）。** 按周期**主动扫描**指定聊天，命中配置的标签后转发到目标并按需下载。规格书 `docs/Telegram标签监听功能_DeepSeek开发任务书.md` 与 `docs/Telegram标签监听_Producer-Consumer_SQLite架构调整_DeepSeek开发任务书.md`。

**最重要的设计约束：标签监听白名单 ≠ 下载白名单。** 两条链路各自独立、互不推导：

```
/wl  下载白名单 ──► 聊天收到媒体 → 实时转发收藏夹 → 下载
listen 标签监听 ──► 定时主动扫描 → 标签匹配   → 转发目标 / 下载
```

监听来源来自自己的 `runtime/listen.json` 与 `state.LISTEN_RULES`，**绝不**从 `state.WHITELIST_CHATS` 推导，也不要求监听聊天加入 `/wl`（一个聊天可以只在一边、两边都在、或都不在）。唯一一次读白名单是 §14 的重叠判定，而且只读：源聊天已在下载白名单时，实时链路收到该媒体就已经转发+下载过，监听就不再为它建「收藏夹」任务（`_effective_work`），只建其余目标。菜单入口也是分开的（§18：`📋 白名单` 与 `📡 标签监听` 必须一眼可辨）。

**数据分层（规格 §4）**：**配置 → JSON**（`listen.json` 是唯一真相）；**业务运行状态 → SQLite**（`runtime/tg_userbot.db`，第一阶段只有 listener 的三张表）；**技术日志 → 文件**（`download.log` 等原样不动）。旧的 `listen_state.json` 已退役，启动时由 `listener.migrate_legacy_state()` 一次性迁入 SQLite（**checkpoint 与 pending 都迁**：pending 那批「未完成的工作」逐项变成 PENDING 任务，丢掉等于那些消息永不转发），成功即改名 `.migrated` 保留人工核对；两条守卫防它帮倒忙——DB 里已有更新的游标就不覆盖（否则会把游标倒回去重复扫一大段），以及坏文件保持原名不动。

**扫描 ≠ 执行（Producer/Consumer）。** 一次扫描可能命中几十条，扫描时直接发就是几十个转发请求砸过去、既容易吃 FloodWait 又没有节流手段，而且进程在中途被杀会把整轮状态丢掉。拆开之后：

```
Telegram ──► listener.Scanner ──► SQLite 任务表 ──► listener_worker ──► 转发 / 下载
```

- **Scanner（`listener.py`）**：读配置 → 按 checkpoint 扫新消息 → 标签匹配 → 多标签合并 → 相册整组 → 算目标与 download → **建任务** → **在同一事务里推进 checkpoint**。**不转发、不下载、不等待 FloodWait、不重试**（§11）。`_SCANNING` 布尔防重入（检查与置位之间无 await，单线程事件循环内安全）。
- **Worker（`listener_worker.py`）**：常驻 `claim → forward → SUCCESS / 退避重试 / FAILED`。**不读 `listen.json`**（§12）——已入队任务的目标以 `listener_tasks` 为准：10:00 建的任务指向 Chat B、10:02 把配置改成 Chat C 之后，那条任务仍发给 Chat B，新消息才发给 Chat C（§19）。
- **Runtime DB（`runtime_db.py`）**：**唯一**的 SQL 出口（§33），其他模块不许散落 SQL。三条硬规矩：SQL 只在本层；**Telegram API 绝不进事务**（事务都是几条 INSERT/UPDATE 的短事务）；**import 期绝不建连接**——`chrome_agent` 也是独立进程、也 import 本包去用 `chrome_client` 的纯函数，若 import 即开库那个进程就成了第二个写者、WAL 的单写者前提当场破坏（有单测钉死「模块顶层不许出现 `sqlite3.connect`」）。

**Schema**：`schema_meta`（schema_version，迁移幂等可重跑）、`listener_checkpoints(source_chat_id PK, last_message_id, updated_at)`、`listener_tasks`、`task_events`。`PRAGMA journal_mode=WAL` + `foreign_keys=ON` + `busy_timeout` + `synchronous=FULL`（都可在 config 调）。**WAL 必须读回实际生效的模式**：Termux 的 `RUNTIME_DIR` 落在 `/storage/emulated/0`（FUSE 外部存储），WAL 依赖 mmap 共享内存、在该文件系统上可能拿不到——不是 `wal` 就回落并**显著告警**，绝不假装生效；真机卡顿可用 `TG_RUNTIME_DB` 把库挪到应用私有目录。`SQLITE_BUSY/LOCKED` 按 §39 做**有限**重试（记日志 → 短暂等待 → 有上限），绝不无限循环、绝不因此崩掉主进程；读操作也收口成 `DbUnavailable` 而**不返回假值**（checkpoint 读失败若返回 0，上层会当成「没有游标」把整个历史扫下来）。

**任务的唯一键与相册**（这是任务书里必须调整的一处）：唯一索引是 `(source_chat_id, message_id, target_type, COALESCE(target_chat_id, 0))`。两处偏离字面规格，都是必须的——① **`COALESCE` 不能省**：SQLite 的 UNIQUE 里 NULL 彼此不相等，按字面 `(…, target_chat_id)` 建的话**收藏夹目标（`target_chat_id = NULL`）那条唯一约束完全失效**，同一消息能插出两条收藏夹任务、转发两次，而收藏夹恰恰是最常见的目标（有单测钉死）；② **`message_id` 存「单元锚点」**（相册取组内最小成员 id），整组成员 id 放 `payload["member_ids"]`：若按成员 id 各建一条任务，Worker 就会逐条转发，收藏夹里相册被劈成 N 条散消息，而 §16 明令保留整组转发行为。`download` 列不在任务书 schema 里但必须有（§22 要求 Worker 转发到收藏夹后调 `enqueue_media`，而 Worker 不读配置，不存这一列就无从知道该不该下载）；`payload`（JSON）另存 `member_ids` 与 `caption`。状态机：`PENDING → PROCESSING → SUCCESS`，重试**不新增状态**（回 PENDING + `next_retry_at`），永久错误 → `FAILED`。

**checkpoint 的语义**（§7/§13）：**「此位置之前需要建的任务都已落盘」**，与「任务是否发送成功」解耦（前者是 Scanner 的事，后者是 Worker 的事）。任务与 checkpoint 在**同一个事务**里提交，只有 COMMIT 成功才推进；失败回滚 → 下一轮重扫，靠唯一索引避免重复建任务。**队列触顶时绝不推进 checkpoint**（§30：没入队的消息不能被跳过），`LISTEN_MAX_PENDING_TASKS`(1000) 是配置项不是硬编码——满了 Scanner 就停，Worker 消费后下一轮继续。

**Worker 的执行纪律**：并发 1 + `LISTEN_WORKER_MIN_FORWARD_INTERVAL_SECONDS` 最小间隔（**这不是 Telegram 官方安全阈值**，只是保守节流；真正的限流以服务端 FloodWait 为最高优先级，§32）。**FloodWait 采信服务端返回值**（§27：不许固定等 60 秒、不许立即重试）**并暂停整个 Worker**——FloodWait 是账号级的，只给那条任务设退避、转头去发别的目标会继续吃限流甚至加重。租约 `LISTEN_WORKER_LEASE_SECONDS`(600s)：claim 落 PROCESSING + lease_until，**Worker 崩溃后租约到期即恢复 PENDING 并记 `LEASE_EXPIRED`**，启动时必须先跑一次恢复（§24）。优雅停机把手上的任务放回 PENDING。错误分类 `classify_error`（`flood`/`permanent`/`retry`）按当前 Telethon 的实际异常类型：`ChatWriteForbidden`/`ChannelPrivate`/`PeerIdInvalid` 等永久错误直接 FAILED 不无限重试（§28），其余有限次退避重试后转 FAILED。**系统保证 at-least-once，不保证 exactly-once**（§29）——SUCCESS 写入前崩溃会重复转发一次，重复下载由现有 `dedup` 拦下（它拦下载不拦转发）。

**扫描纪律**：按 chat 扫描一次再匹配该 chat 全部规则（§9）；同一消息命中多标签/多规则 → 先「匹配 → 合并」（目标是**集合**），再按 (单元, 目标) 各建一条任务，同一目标只建一次（§14/§15）；相册按 `grouped_id` 整组为一个单元，**并补齐被一轮条数上限切开的边界组**（`_complete_boundary_group`：limit 是从最新一侧切的，不补就会只转发半个相册——这是改造前就存在的真 bug）；只处理**媒体**消息（纯文本转发进收藏夹会被 `app._record_me_label` 当成「待关联评论标注」拼进下一个下载文件名，污染命名）；首启以当前最新消息 id 作 checkpoint **绝不扫历史**（§17），新增标签沿用同一聊天的 checkpoint、不会回头扫历史（§18）。

**download=true 的实现 = 转发到收藏夹 + 入队那份转发副本**，复用 `app.enqueue_media` → 下载队列 → dedup → 命名的整条链路，**不新增第二套下载系统**（§22）。为什么要显式入队而不是指望收藏夹的事件入口：**userbot 自己发出的转发不会回流成更新**（2026-09-11 用生产日志核实：转发出的副本 ID=31160 在 208 条「📨 Saved Messages 收到消息」里没有对应行），这正是既有白名单链路 `_relay_single` 转发完也要显式 `enqueue_media(fwd, …)` 的原因。转发本身仍必要：副本自带系统「转发自 <来源>」头，落盘目录由副本的 `fwd_from` 解析回原来源频道。`download=false` 且 targets 显式含 me → 只转发不入队。

**稳定性**：每聊天独立 try/except（`@source1` 失败不影响 `@source2/@source3`，§23）；所有网络调用经 `netio.shielded` 收口（断线的网络层取消只会变成「本轮没做成」，绝不把后台循环打死）；单个任务失败绝不让 Worker 退出（§42）；后台任务保持强引用（§35）；数据库不可用时整轮放弃且 checkpoint 原地不动。配置读失败回落空配置、写盘 temp + `os.replace` 原子；`reload_listen_config` 每轮按 mtime 按需重读（§17 改配置无需重启）且**只在解析成功时才替换内存态**——半截文件把规则清空后，用户下一次点任何按钮就会把空规则保存回文件，规则就真没了。

**入口**：后台任务 `app._listener_loop()`（Scanner，启动延迟 `LISTEN_STARTUP_DELAY_SECONDS` 后扫一轮再按周期）+ `listener_worker.start_worker()`（常驻），启动顺序 `init_db → migrate → migrate_legacy_state → recover_expired → worker → scanner`（§36）；DB 起不来则标签监听整体不启动、下载等其余功能不受影响。菜单 `📡 标签监听`（视图 / ➕添加 / ✏️修改 / 🗑删除 / ▶️立即扫描 / ⏱周期 / 🔄开关 + 任务队列存量），命令 `/listen [on|off|scan|interval <分钟>|del <序号>|add <聊天> <标签> [目标,目标] [on|off]]`，菜单动作在 `MENU_ACTIONS`（漏登记会让按钮静默变成 unknown）。添加/修改走**多步向导**：来源聊天（输入窗口）→ 标签（输入窗口）→ 目标勾选（按钮，可 ➕ 加聊天）→ 下载开关 → 保存；四个输入窗口（cookie / 查询 / Caption 清洗 / 标签监听）由 `bot.open_input_window` 统一互斥。

**台账与汇报**：`enqueue_media`/`queue.enqueue_and_start` 有可选 `src`，监听传 `"listen"`——否则监听副本的 chat_id 也是我，跟用户在收藏夹手动转发完全分不开。台账 `📥 输入事件` 因此显示「收藏 / 中转 / 监听」三桶（相加 = 收到媒体总数，可勾稽），并新增 `📡 标签监听` 分节：扫描/命中/入队任务（Scanner 口径）**+ 任务结果（成功/失败/待执行，从 SQLite 读，§37）**；只在窗口内真扫过时才出现，老日子输出不变。事件 `LISTEN_SCAN`（每聊天一次成功扫描，带 `scanned/matched/created/duplicate`）/`LISTEN_FAIL`（单聊天读失败，**独立事件**——否则失败的聊天会把「扫描 N 次」算虚高）都是无 task_id 的输入侧事件，不参与任务集构造、不扰动对账恒等式。**注意 SQLite 的 `task_events` 与 `runtime/task_events.jsonl` 是两套 ID 空间、同名不同物**（前者是 listener 任务的自增 id，后者是下载任务的 uuid），绝不混用：SQLite 事件只服务 listener 自身排查，喂给 reporter/台账 的永远是 JSONL。Reporter 面板加一行只读快照（`state.LISTEN_LAST_SCAN`，含队列存量；Reporter 是只读观察者，不碰数据库不做网络），事件通知按批聚合且**只在有命中或失败时发**（空扫也发就成固定刷屏）。单测 `tests/test_runtime_db.py`（schema/迁移幂等/WAL 与 PRAGMA 如实反映/checkpoint/UNIQUE 含 **NULL 收藏夹目标**去重/事务回滚 Case A·B/claim·complete·retry·fail·cancel/lease 恢复 Case C/BUSY 有限重试/事件外键）、`tests/test_listener_worker.py`（错误分类与退避/FloodWait 采信服务端值并全 Worker 暂停/临时与永久错误/重试耗尽/租约恢复/节流/优雅停机/强引用/单任务失败不退出）、`tests/test_listener.py`（标签边界/目标身份/相册分桶/规则校验/配置持久化含重载保护/规格书 §27 测试 A–G/队列上限背压 Case E/相册边界补齐/旧状态迁移含「不倒回更新游标」/命令与视图），回归在 `test_stats.py`、`test_reporter.py`、`test_menu.py`、`test_commands.py`、`test_cookie_menu.py`、`test_cleanup.py`。

**Concurrency model.**
- `DOWNLOAD_SEMAPHORE` (custom `AdjustableSemaphore`, default limit 3) bounds concurrent downloads — every media download (Saved Messages originals, whitelist-relayed copies, douyin/IG video copies) shares the pool. Unlike `asyncio.Semaphore`, the limit can change at runtime: `/thread n` calls `set_limit()` (range 1–25), persists to `thread_config.json`, and is loaded at startup via `load_thread_config()`. Reducing the limit does not cancel in-flight downloads; new tasks wait until the count drops below the new limit.
- `PROCESSING_DOUYIN_IDS` (a set of message IDs) prevents duplicate link relays for the same Saved Messages message (no conversation/no per-platform lock anymore — the parse bot is reached by plain `send_message`, so nothing needs serializing); IDs are discarded in a `finally`.
- Progress/attempt log lines include the target filename since parallel downloads interleave in the log.
- In-flight downloads are tracked in `ACTIVE_DOWNLOADS` (plain dict keyed by an incrementing id) via `register_download`/`update_download`/`unregister_download`; `download_file` wraps its body in `try/finally` around the registration, and the `progress_callback` calls `update_download` before the throttled log line. `/progress` reads this dict directly (no lock needed — single-threaded event loop).

**多 worker 并行下载池（N 条独立连接并发拉文件）。** Telethon 每个客户端只有一条共享发送 socket（`_sender`），同一条 socket 上的文件下载分块串行——这是多 socket 并行的**必要条件，但并非当年 ~220KB/s 瓶颈的根因**：实测 10 条 worker 连接在 `obfuscated` 传输下聚合仍 ~213KB/s，因为每帧 AES-CTR 走单核纯 Python pyaes（真天花板，见上方 Transport 默认的说明）；换成 `full` 传输后同样 10 worker → ~2.3–2.6MB/s（≈12×），worker 才真正并行。多 worker 池（`workers.py`）给每个进行中下载配一条**独立连接**（worker），聚合先随 N 线性涨、后到墙（实测单条 ~0.3MB/s；本机经 Hiddify 到 TG DC 聚合封顶 ~2.3–2.6MB/s ≈ 21Mbps，10 路与 24 路同速，墙在 ~8–10 路附近——位置取决于线路/代理节点/TG 路径）。所以 /thread 10 即跑满当时那台 Hiddify 节点；但上限按用户要求保留到 25（节点带宽时快时慢、瓶颈在节点总带宽不在 bot），何时用 10 还是 25 由用户在运行时自定——慢节点开多只摊薄单文件速度、不加聚合，快节点开过 10 可吃更高聚合。worker 是下载专用瘦客户端：从主客户端内存 session（`state.client.session`）拷 dc_id/server_address/port/auth_key 喂 `MemorySession`（零磁盘写、不抢 SQLite 写竞争），`receive_updates=False`（connect 的初始化请求与发出的请求都包 `InvokeWithoutUpdates`，Telegram 不给 worker 推更新流 → 主客户端仍是唯一更新消费者；worker 也没注册事件处理器），只 `connect()` 不 `.start()`（auth_key 已拷入即已授权）。同一账号同 auth_key 多连接 = 官方客户端多 socket 语义，不产生新登录会话。**`/thread n` 的语义现为「并行下载路数 = worker 连接数」**：`DOWNLOAD_SEMAPHORE`（限 1–25）与池目标 `state.DOWNLOAD_WORKER_TARGET` 恒同值同变——`apply_thread_limit` 调 `set_limit()` 后再调度 `workers.sync_pool_to_target()` 即时加/减连接。路由内聚在 `download_file`（`queue.py` 不改）：进入下载先拿信号量槽（≤n 个进行中），再 `workers.borrow()` 借一条连接传字节（`worker.download_media(message, ...)` —— 消息 media 的 dc_id/access_hash/file_reference 内嵌、无需 worker 解析实体；文件在其它媒体 DC 时按需 export 授权，对任意已连客户端成立）。同一账号多条 worker 首次跨 DC 下载是「并发首次 export 授权」——Telegram 会同时判绝大多数 `AuthBytesInvalidError`（谁先落地谁赢；实测 6 路并发首轮就有 5/6 失败）。但每个 worker 的成功导出按 DC **永久缓存**、此后该 worker 不再导出（热缓存带池复用），竞态只是冷启动瞬态、随成功者退出自然消散；download_file 只对这类**秒级、首字节前**的快速失败放宽重试上限（`DOWNLOAD_RETRIES + EXPORT_RACE_EXTRA_RETRIES`，见下载模式），普通失败仍按 `DOWNLOAD_RETRIES` 封顶。池未启用（spawn 全失败 → `DOWNLOAD_WORKER_QUEUE=None`）时 `borrow()` 返回 None，download_file 照旧走 `message.download_media`（测试与降级路径不变）。`sync_pool_to_target` 缩容只摘「空闲」连接断开、不打断在途下载；live 恒不低于 target（信号量先于借 worker、下载并发数 ≤ 存活 worker 数）→ borrow 不饿死；`release()` 归还时顺手收敛超额空闲（多次升降不累积闲置连接）。**`borrow()` 借出前验活**（2026-09-07 补）：池没有自动重连守护，空闲停靠期被网络抖动杀死的连接就地重连（`_WORKER_CONNECT_TIMEOUT` 超时防代理黑洞挂死借出）再借出；重连失败（网络仍断）仍照常借出、由下载自身重试（`_sleep_and_reconnect`）兜底 —— 扣下/换走会把池抽干饿死借方。直接起因：当天 04:00 代理断连把 20 条 worker 全数杀死在池中且无人复活，之后每个下载任务第 1 次尝试都 0 秒败在「Cannot send requests while disconnected」、白烧一次重试预算，其中 3 个任务再被网络杀两次就耗尽重试永久进 retry 列表。worker 客户端与空闲队列也只在 `app.main()` 构建（登录成功后、`recover_queue_tasks()` 前 `spawn_pool`；事件循环规则），退出时 `workers.shutdown()` 尽力断开。文件字节只走 worker，通知/来源解析/重连等主链路仍留 `state.client`（重连用当前传输客户端 = worker 或 state.client）。单测在 `test_workers.py`（池簿记 + download_file 的 worker 路由分流；不联网）；`test_queue.py` 死锁回归在无池环境跑（borrow 返回 None → 原路径，不受影响）。

**Download pattern (single shared path).** Every download goes through `download_file` (`download.py`): download to `final_path + ".download"`, then `os.replace()` to the final path for atomicity. Retries with a 3-second delay, reconnecting the client between attempts if needed; the attempt cap is per-error-kind — ordinary failures give up after `DOWNLOAD_RETRIES`, but the cross-DC first-export race (`AuthBytesInvalidError`, see the 多 worker section) is a cheap pre-byte fast-fail that converges as winners drop out, so it is allowed `DOWNLOAD_RETRIES + EXPORT_RACE_EXTRA_RETRIES` attempts (config.py). Each attempt is wrapped in a **no-progress watchdog**: Telethon requests have no read timeout, so a stalled proxy yields no bytes and no error — if no `progress_callback` fires within `DOWNLOAD_IDLE_TIMEOUT` (120s) the attempt is cancelled and raised as `TimeoutError` (→ retry branch). Network-layer cancels are discriminated from genuine ones structurally: telethon's mtprotosender calls `future.cancel()` on pending request futures when disconnected, and since py3.8+ `CancelledError` is a `BaseException` it used to bypass `except Exception` and kill the queue task silently (the "队列有 6 个任务 /progress 却空" bug — record stayed forever in `tasks`). Because a network cancel only surfaces through the child download task's synchronous `.result()`, it is converted there to `ConnectionError` (retryable); any `CancelledError` that reaches the retry-loop `except` is thus a genuine `Task.cancel()` (process exit) and is re-raised so the record stays in `tasks` for restart recovery. The outer `finally` **always** removes the `.download` temp, releases the worker, unregisters `/progress`, and frees the reserved path — every exit path, success or cancel, leaves no orphan temp. The final path is claimed by `_reserve_final_path` before downloading: an in-process registry (`_RESERVED_FINAL_PATHS`, released in `finally`) plus `os.path.exists` disambiguation means two concurrent downloads that compute the same basename — e.g. same-album photos sharing one inherited `album_caption` and meaningless original names — never share a `.download` temp (which would make one fail with "临时文件不存在"). `/clean` deletes leftover `.download` files; `main()` also runs `clean_temp_files()` once at startup (safe — nothing is downloading yet) to catch orphaned temp files from abnormal exits or cross-version filename changes.

**Persistent download queue.** All downloads run through a queue persisted in `RUNTIME_DIR/download_queue.json` (`{"tasks": [...], "retry": [...]}`, file order = FIFO execution order). There is exactly one task kind now — `media`, a message reference `{kind, id, chat_id, msg_id, source_override, label, final_name, source_link}` (Saved Messages originals and whitelist-relayed copies alike), with an optional `album_caption` (相册无文字成员继承到的同组说明) set only when a grouped member carries no own text, and an optional `user_label` (手工转发评论标注，见「手工转发评论 → #标注命名」) set when the media inherited a pending comment — both used by download/display naming (`download_file(..., caption_override=record["album_caption"], label_override=record["user_label"])`). The persistent `id` (uuid hex) doubles as the **全链路 trace id** (见 Logging 段). `enqueue_and_start()` appends the record, saves under `QUEUE_LOCK` (an `asyncio.Lock` created in `main()`), and spawns `execute_queued_task()` via `spawn_execute` (holds a strong task ref against GC — `_SPAWNED_TASKS`). **The queue layer deliberately does NOT acquire `DOWNLOAD_SEMAPHORE`** — `download_file` already acquires it internally, and a second acquisition around it deadlocks when concurrency ≥ 2 (all slots held at the outer level while every task waits for an inner slot; regression-tested with a limit-1 semaphore). Actual file-download concurrency is still bounded by the internal semaphore. Media tasks re-fetch the message on a **child task** polled by `asyncio.wait` with `QUEUE_FETCH_TIMEOUT` (30s) — deliberately not `asyncio.wait_for`, whose injected cancellation would be indistinguishable from a genuine `Task.cancel()`; a network-cancelled child (telethon `future.cancel()`) is caught at its synchronous `.result()` and treated as ordinary failure → retry, while a genuine cancel propagates so the record stays in `tasks`. A deleted original message removes the task with a notification (permanent failure). On success the record is removed; on failure it moves to `retry` with `attempts+1` and a `next_retry_at` stamp (见下「retry 榜到期自动重放」). The user can still re-trigger manually via `/retry <n>`, `/retry all`（一键重放全部，EXECUTING 中的跳过；`queue.retry_all()`，菜单 🔁 视图【♻️ 全部重放】同路）or the menu's ▶️ button (success removes it, failure leaves it in place) — **手动路径不看退避与次数上限，立即执行**。

**retry 榜到期自动重放（2026-09-10，issues/002）。** 起因：链路分钟级抖动时在跑任务 3 次尝试必然全撞上、快速入榜后原本「永久停靠」等人肉 `/retry all`（实测 2026-09-10 一早晨堆到 44 条）。现在失败入榜时写 `next_retry_at = now + min(AUTO_RETRY_BASE_DELAY × 2^(attempts-1), AUTO_RETRY_MAX_DELAY)`（60s→30min 封顶，`queue._backoff_delay` 纯函数），`app._retry_sweeper()` 后台任务每 `AUTO_RETRY_SWEEP_SECONDS`(60s) 调一次 `queue.replay_due()`：只放到期、非 EXECUTING、且 `attempts <= AUTO_RETRY_MAX_TIMES`(10) 的记录——**超过 10 次不再自动重放，只能人工救**（防坏窗口无限空转）。**每轮最多放 `_idle_capacity()` 条 = 当前空闲 worker 数**（`DOWNLOAD_WORKER_QUEUE.qsize()`；池被禁用时回落 `DOWNLOAD_CONCURRENCY`）：一次放太多会撞并发首轮跨 DC 授权导出竞态（实测 6 路并发 5/6 首轮失败，见 `EXPORT_RACE_EXTRA_RETRIES`）。`replay_due` 的「查到期 → spawn」之间无 await、`EXECUTING` 在任务入口第一个 await 前已写入 → 不会重复放行。**向下兼容**：旧记录没有 `next_retry_at` 字段视为已到期，故升级/重启后旧榜会自动重放一轮（自愈），此后按退避节流。`queue_retry_failed`（retry 内再失败）同样刷新该字段。无运行时开关（要停改常量重启）。 **列表分页**（2026-09-08）：`format_retry_text`/`format_queue_text` 按 `LIST_PAGE_SIZE`(10)/页渲染——60+ 条全量渲染会超 Telegram 4096 上限、回复直接发不出（「点重试列表没数据返回」的根因）；页头带「共 N 条，第 x/y 页」统计，序号为跨页全局序号（/retry <序号> 语义不变），菜单 retry 视图带 ◀️/▶️ 翻页与页码刷新按钮。 `EXECUTING` (set of record ids) prevents double-triggering. On startup `recover_queue_tasks()` re-spawns everything in `tasks` (interrupted downloads restart; leftover `.download` temp files are cleaned by the existing retry logic). Historical `douyin`/`instagram` records that survived from before the unified-chain refactor hit the "unknown kind → remove + log" safety net in `_run_queued_task` (never crash or wedge; `QUEUE_KIND_LABELS` still renders them readably until removed). Pure queue functions (`load_queue`, `save_queue`, `queue_enqueue`, `queue_fail_to_retry`, `queue_remove`, `queue_retry_success/failed`, `format_queue_text`, `format_retry_text`) and the deadlock/cancellation regression tests are in `test_queue.py`.

**Platform link flow (Douyin + Instagram, relay-only).** `extract_douyin_urls`/`extract_instagram_urls` both delegate to `extract_urls_by_pattern` with their own regex (douyin covers v./www./m./douyin.com; instagram covers instagram.com/instagr.am with optional www.). When a Saved Messages message carries such a URL, `platform.relay_platform_links` (deduped per message id by `PROCESSING_DOUYIN_IDS`) simply sends the link text to the parse bot chosen per kind from `PLATFORM_LINKS` (douyin and instagram both use `@DouYintg_bot`; the dict carries only the bot username + a log label). Nothing is queued and nothing is self-downloaded: the parse bot's first direct-video reply is consumed by the whitelist relay (item 4 above) — because the parse bot's private chat is on the whitelist — and lands in Saved Messages for the normal media download. Best-effort, `_relay_kind_links` resolves the parse bot's entity and warns if it is not in `WHITELIST_CHATS` (its replies would otherwise never be forwarded); a send failure logs and notifies "me" with a `❌ 抖音链接处理失败`/`❌ Instagram链接处理失败` message (auto-cleaned).

**本地解析链（桌面端，f2 优先 + bot 兜底，2026-09-06 起）。** 抖音链接不再单点依赖解析 bot：`platform.relay_platform_links` → `_handle_douyin_urls` 在 `config.RESOLVER_ENABLED`（= 非 Termux 且 `TG_RESOLVER != off`）时先走 `resolver.resolve_douyin(url)`——f2 的 `AwemeIdFetcher.get_aweme_id` 展开分享短链、`DouyinHandler.fetch_one_video` 拿作品详情、`pick_best_video_url` 从 `_to_raw()` 的 `aweme_detail.video.bit_rate` 列表挑**最高码率档**的直链（过滤器便捷属性 `video_bit_rate` 只给数字，档位 URL 必须读 raw）。成功 → `build_url_record` 组 `kind="url"` 队列任务（`final_name` 入队时经 `compute_url_filename` 算好持久化；`source=抖音` → 落 `SAVE_FOLDER/抖音/`），`download_url_media` 用 httpx 流式下载——共享信号量/.download 临时文件+原子改名/无进度看门狗/失败转 retry/历史记录的全套纪律，但不借 worker（HTTP 不走 Telethon 连接池）。**直链过期防护（2026-09-07，刷新 + 降级 bot）**：douyinvod 直链签名只活 ~3 小时（实测 = 解析时刻 +3h，过期戳以 hex 内嵌在 URL 的 `/<32hex md5>/<hex>/` 段），深队列排队太久会拿着过期直链开下——旧行为是 403 白烧 `DOWNLOAD_RETRIES` 次转 retry，而 `/retry` 重放的仍是同一条死链（任务救不回，只能重发链接）。现在 `download_url_media` 三层防护：① 开下前（占信号量前）`direct_url_needs_refresh` 解码内嵌过期戳，剩余寿命 < `DIRECT_URL_REFRESH_MARGIN_SECONDS`（15min，config）就 `_refresh_direct_url` 用记录里保存的**原始分享链接**重新解析，新直链原地写回 `record["direct_url"]`（记录对象与队列里同一份，任务收尾 `execute_queued_task` 的 save_queue 顺带落盘，转 retry/重启重放受益）；签名格式认不出就不盲刷、留给 403 兜底。② 尝试中收到 403/410（`_DIRECT_LINK_GONE_STATUS`）**短路重试循环**：没刷新过就补一次刷新换新直链立刻重下（`continue`，不耗重试预算）；刷新过仍被拒或拿不到新链 → ③ `_delegate_url_task_to_bot` 把原始链接经 `platform.relay_links_to_parse_bot`（`_relay_kind_links` 的公开入口）转交解析 bot 兜底并返回 `"delegated"`——bot 在自己服务端解析、不依赖本地 cookie，这条兜底永远可用；truthy 的 `"delegated"` 让队列按成功移除任务（bot 回复视频走白名单流另入队，移除防止重放死链/双下载），转交本身失败（网络断）才返回 False 转 retry 不丢链接。其它 HTTP 错误（5xx 等）与降级无关，照旧 `DOWNLOAD_RETRIES` 普通重试。cookie 失效与直链过期是两个独立的钟（cookie 存活以天/周计、直链 3h），refresh 失败才轮到 bot 兜底。单测在 `tests/test_url_refresh.py`（httpx.MockTransport + mock resolver/relay，不联网；命名排 test_menu 之后 + patch append_history，避免假下载污染共享 SAVE_FOLDER 的「今日 N 个」统计断言）。任何失败（f2 未装/import 抛错/签名过期/网络断/`RESOLVER_TIMEOUT_SECONDS`=30s 超时）→ 返回 None → 剩余链接原样走 `_relay_kind_links` bot 中转，最坏情况 = 维持现状。**f2 的 import 本身会联网取 msToken**（pydantic 字段默认值），所以 import 也包在降级 try 里、且在 `_handle_douyin_urls` 内延迟导入；f2 可选依赖（`pip install f2`，自带 httpx），没装只警告一次。抖音 Web cookie 从 `tg_secrets.json` 的 `douyin_cookie` 字段读（缺省空串大概率失败 → 自然降级），也可在 bot 菜单【🍪 抖音Cookie】里更新/清除——等待窗口（`COOKIE_INPUT_WINDOW_SECONDS`=120s）内发的文本即 cookie，原文立即删除，`config.save_douyin_cookie` 原子写盘（保留其它字段）并同步更新内存 `config.DOUYIN_COOKIE`（resolver 每次解析动态读，实时生效）；【🌐 Chrome/Edge/Firefox】按钮经`browser_cookies.load_browser_cookie_string`（f2 自带 browser_cookie3 直接读本地加密 Cookie 库、含 HttpOnly sessionid；注意版本需 ≥0.20.1——f2 拖进的 0.19.1 解不开新版 Chrome（≥v11 加密格式）的 cookie，会报 Unable to get key for cookie decryption，需手动 `pip install -U browser_cookie3`；Firefox 读取前需完全退出；菜单侧 run_in_executor 调用防钥匙串授权框阻塞事件循环）一键导入 douyin.com cookie，CLI 同款 `scripts/import_douyin_cookie.py`；分享口令碎屑（'复制打开抖音…'等标记词、>60 字剩余文本）不会被误当标注。Instagram 始终走 bot（匿名本地解析不可行）。真实解析用 `scripts/smoke_resolver.py <分享链接>` 冒烟（联网，不进常规单测）。

**Naming rules.** `sanitize_filename` strips Android/Windows-illegal characters; `unique_path` appends ` (1)`, ` (2)` on collision; `get_file_extension` infers extension from MIME when the original name lacks one. The whole naming decision lives in the pure `compute_final_filename(message, caption=None, label=None, max_bytes=None)` (shared by `download_file` and queue display; `caption` overrides the message's own text so album copies can inherit the group caption recorded as `album_caption`; `label` is the 手工转发评论标注 — the code renders it `#<label>` and splices it in front of the caption, e.g. `26-09-06 #自存 标题… - 原名`; `max_bytes` non-`None` = download-time byte budget, below) — there is no platform-specific short name anymore, so douyin/IG video copies relayed into Saved Messages use the same generic naming. Since the relay refactor, every name starts with the **original message date prefix** `YY-MM-DD ` (`date_prefix(message)`, e.g. `26-09-05 `), which chronologically sorts each source folder and prevents same-caption collisions; the only exception is the pure fallback name `媒体类型_时间戳` (already carries the date). 2026-09-06 起 `date_prefix` 对转发副本取的是**转发来源的最初日期**（`message.fwd_from.date`，如频道原帖的发布日），不是转发当天——只有 `fwd_from` 缺失/缺日期时才退回 `message.date`（收藏夹自发送即此路径）；日期是 UTC 感知时间，按本地时区渲染（= 客户端里看到的日期）。注意抖音/IG 解析 bot 的视频例外：其副本的 `fwd_from` 指向 bot 回复消息，原始日期只能是 bot 回复时间（≈发链接当天），抖音真实发布日期不在 Telegram 元数据里。 Files save as `<date> <caption> - <filename>` in `SAVE_FOLDER/<forward-source-name>/` (source resolved via `resolve_download_source()` → `get_forward_source` → `client.get_entity` from the message's `fwd_from` — whitelist-relayed forwards, Saved Messages originals, and user-forwarded copies alike all carry `fwd_from`, so every download resolves its folder the same way). `get_caption(message, override=None)` is the single resolution point for the naming caption: it takes the **raw** message text (or the caller's album-inherited caption) → `clean_caption` (Caption 命名清洗，见上) → `sanitize_filename`, and returns `""` for captionless messages (never `sanitize_filename("")`'s fallback "未命名文件", which used to prefix every captionless file) — cleaning runs **before** sanitize, exactly once (`raw_caption()` is the uncleaned accessor used for the own-vs-inherited preference in `download_file`). Meaningless names (未命名/UUID/32-hex, detected by `is_meaningless_filename`) are replaced with `媒体类型_时间戳` (or caption alone when present — including an inherited `album_caption`, which flows through the same rule); a meaningful `#label` alone (no caption) also rescues the meaningless case (`<date> #自存.mp4`). **超长名的裁剪优先级** (下载时 `max_bytes=MAX_FILENAME_BYTES` 传入)：拼出超限名时按「保 日期前缀+真实文件名/扩展名 → 其次才动 #标注 → 最先从原 caption 尾部整字裁」的顺序收敛——早年 `truncate_filename(整名)` 会先从文件名一侧截，恰与需求相反，故 download 不再用它、改由 `compute_final_filename` 内部按预算组名。A size sanity check logs a warning when the downloaded size is <98% of the message's declared size (client may show pre-transcode sizes).

**手工转发评论 → #标注命名（2026-09）。** 转发带 caption 的媒体到收藏夹时若在输入框打了句话，Telegram **不合并进副本 caption**，而是发成「评论在前、媒体紧跟」两条（多转发则一条评论 + N 条媒体）。app.py 在 Saved Messages 收到一条纯文本（非命令/非链接/非程序消息，`cleanup.is_cleanup_message` 判定）时记作待关联标注（`_record_me_label`，module 级全局 + monotonic 时间戳）；`_enqueue_me` 在 `ME_LABEL_WINDOW_SECONDS`（config，5s）窗口内取用（`_take_me_label`），**取用不清空** —— 一条评论后连续转发的 N 条媒体都继承同一条标注，靠时间自然过期。标注随队列记录持久化为 `user_label`，经 `queue._run_queued_task` → `download_file(label_override=…)` 传入命名。窗口语义单测在 `test_label_capture.py`。

**重复媒体去重（2026-09-07，三级防线 2026-09-08 起）。** 同一频道帖子转发两次 / 同一抖音视频换口令重发会原样再下载一遍（几个 GB 白吃）。判重键按防线层级分四类，全部走同一份 append-only 索引（`runtime/dedup_index.txt`，`key\t日期\t文件名` 行格式，键前缀自区分）：

- **元数据级 `tg:<file.id>`**（入队时）：telethon `File.id` = document/photo 的服务端文件 ID，同一媒体转发到任何聊天不变——「同一帖子转两次」的判据。**不能用 `file.unique_id`**：telethon 1.44 的 File 没有该属性，取它恒 AttributeError → media_key 静默返 None → 去重对真实消息整体失效（2026-09-08 实测 81 个下载 0 入索引的根因，回归测试钉死了真实 File 形状）。
- **元数据级 `dyc:<aweme_id>`**（入队时）：抖音本地解析，不同分享口令指向同一视频也判得住。
- **文件级 `f:<原始文件名>:<字节大小>`**（入队时，2026-09-08）：判「**bot 重传**」——解析 bot 每次重新上传 file.id 都变，但 bot 生成的文件名与字节大小不变。原名无意义（未命名/UUID，含没有文件名的照片）不参与（`is_meaningless_filename`，不同文件可能恰好同大小会误伤）。`dedup.file_key`/`media_keys(message)` 返回键列表。
- **内容级 `c:<sha256>`**（下载完、`os.replace` **前**，2026-09-08）：读回 `.download` 临时文件分块算哈希查索引（`download._hash_file` + `_content_dedupe_check`）；url 任务在 httpx 流式循环里逐块 inline 喂哈希（零额外 I/O），媒体路径事后读回。**命中 → 删临时文件不落盘**（`.download` 扩展名不在 CD2 备份白名单 → 重复内容进不了 115）、通知「⏭️ 内容重复已拦截」、**把 tg:/f:/dyc: 元数据键补记进索引**（下次同内容连元数据层都能拦，防线越用越强）、按成功收尾（队列移除记录、无 history 行）；未命中 → 正常落盘 + 三键一并 remember。台账「去重」桶计 `内容重复已拦截` 子串——该出口的任务既无成功行也不转 retry，不计数则勾稽恒等式破。

**入队前判重**（`dedup.should_skip`，收单键字符串或键列表——列表任一命中即拦）：① `state.DEDUP_INDEX`（已下载索引，启动 `load_index()` 载入）；② 扫 `state.QUEUE` tasks/retry 记录的 `dedup_keys` 列表字段（在途/待重试也算重复；旧记录的单字段 `dedup_key` 兼容匹配，抖音 url 任务仍用单字段）。**检查点各一处**：媒体在 `app.enqueue_media`（收藏夹原件与白名单转发副本的唯一汇合点，相册整组转发也走它，记录存 `dedup_keys` 列表），抖音 url 在 `platform._handle_douyin_urls` 解析成功后、入队前（单 `dedup_key`）。**命中只拦下载不拦转发**——白名单转发的「转发自」副本照旧留在收藏夹当书签，收藏夹直发的重复媒体直接不入队；通知「⏭️ 重复媒体已跳过下载 / 相同文件已跳过下载（文件名与大小一致）」进自动清理白名单。`/dedup off|on` 总开关（`dedup_config.json` 持久化，默认开）：off 时三级全放行、照常 remember（off 期间下载的内容也挡住未来的重复）；想重新下载已删到 115 云端的文件时临时关掉。**索引存储 = append-only**：`remember()` 成功下载后在 `download_file`/`download_url_media` 落盘处单行追加（多键逐行）——与 download_history.txt 同款「事件循环内单行 append 原子」纪律，**永不全量重写**；只在启动 `load_index()` 裁剪到 `DEDUP_MAX_ENTRIES`（1 万条，保尾部、原子重写一次），内存峰值 ~1.5 MB。**判重不确定性永不拦下载**：file 缺失 / 大小缺失 / 原名无意义 / aweme_id 拿不到 / 哈希算不出 → 无键 → 放行。**已知局限**：内容级只能兜住「功能上线后下载过」的内容——索引无历史回填（115 上已搬走的存量无指纹，本地上存量也未 hash；曾做过 /dedup rebuild 收藏夹回填，2026-09-08 按用户要求移除），上线前下载过的内容再转发仍会重下一次、但下载完会被内容级拦住不再重复落盘。单测在 `test_dedup.py`（四类键/多键 should_skip/索引/开关/接线）、`test_workers.py`（download_file 内容拦截全链路）、`test_url_refresh.py`（url 内容拦截/落盘）。

**Caption 命名清洗（2026-09-10）。** 转发说明常带结构化字段与 URL（`作者：#腿玩年 期数：bl11 角色：#弱音 i站地址【 https://… 】 标签：#MMD #掉装备`），整段塞进文件名又长又噪。`caption_filter.clean_caption()` 按**可配置规则**只清洗命名用的说明文本，输出再交给既有命名逻辑（超长仍由 `naming.truncate_filename`/字节预算收敛，清洗**不做截断**；清洗后为空也不产生任何 fallback 文本——无 caption 的原有行为不变）。

四类规则前缀，规则是普通字符串列表（`config.DEFAULT_CAPTION_FILTER_RULES` 为默认值）：`exact:<串>` 删这个确切子串；`contains:<串>` 删所有出现（**有意宽匹配**）；`regex:<正则>` 按正则删（非法正则跳过并告警，绝不影响下载）；`field:<字段名>` **剥标签、留值**——`作者：#腿玩年` → `#腿玩年`。**执行顺序恒为 field → exact → contains → regex → 空白归一**（同类型之间保持用户配置顺序）：field 依赖 Caption 原始字段结构，必须最先跑，否则 `contains:作者` 会把 `作者：#腿玩年` 的结构先打碎。field 的字段起点要求落在开头或空白之后（`(?<!\S)`），故 `这个作者真的很厉害。`／`我认识一个作者：张三。`／`第2作者：x` 这类自然语言不会被误删；字段名后**紧跟左括号**也算字段起点，覆盖 `i站地址【…】` 这种没有冒号的写法（括号内容交给 regex 规则）；字段名一律 `re.escape`（用户可配 `field:A+B`）。空白归一只把连续空白（含换行）压成一个空格，`#MMD #掉装备` 不会变成 `#MMD#掉装备`。

**接入点与「只清洗一次」**：清洗的唯一入口是 `naming.get_caption(message, override=None)` —— 取**原始** `message.message`（或调用方传入的相册继承说明）→ `clean_caption` → `sanitize_filename`，`compute_final_filename` 两条来源（消息自带文字 / `album_caption`）都走它，而入队展示名与落盘名都走 `compute_final_filename`，故两处天然一致、全链路只清洗一次。**清洗必须在 sanitize 之前**：`sanitize_filename` 会把换行和 ASCII 冒号 `:` 换成 `_`（实测 `作者:张三` → `作者_张三`、`作者：#腿玩年\n期数：bl11` → `作者：#腿玩年_期数：bl11`），之后字段边界就再也认不出来了。因此 `download.py` 只做「谁的文字优先」的选择、取原始文本（`naming.raw_caption`），清洗交给 `compute_final_filename`。**抖音本地解析链的 url 任务标题（`compute_url_filename`）不走清洗**（那是解析元数据 desc，不是 Telegram caption）。

**配置与 Bot 入口**：规则存在 `state.CAPTION_FILTER_RULES`（当前值，`clean_caption` 每次调用都读它 → 改完**实时生效、无需重启**），持久化在 `runtime/caption_filter.json`（原子写，`load_caption_filter_config` 启动时在 `app.main` 载入、逐条校验丢弃坏规则）。默认值 `config.DEFAULT_CAPTION_FILTER_RULES` 与当前值分离，`reset` 恢复的是**默认常量**而不是「再读一遍当前值」。入口两条、共用 `caption_filter` 的服务函数：`/caption_filter`（`commands.handle_command` 分发；无参=列表、add/del/clear/reset/test）与主菜单【🧹 Caption 清洗】（查看/添加/删除/测试/恢复默认/清空，挂在【🔍 查询】同一行）；添加/删除/测试复用 cookie 那套等待输入窗口（`state.CAPTION_INPUT_UNTIL`+`CAPTION_INPUT_MODE`，`bot.open_input_window` 统一开关且**三窗口互斥**——否则先开的 cookie 窗口会把一段规则当 cookie 存进 tg_secrets.json）。`/caption_filter` 命令与「🧹 Caption 清洗」/「🧪 Caption 清洗」等回复前缀在自动清理白名单里。单测在 `tests/test_caption_filter.py`，另有 `test_commands.py`/`test_menu.py`/`test_cleanup.py` 覆盖命令分发、菜单与输入窗口、清理白名单。

**Display details.** `/queue`, `/retry`, and the menu views show each task's final filename (stored at enqueue time as `final_name`, always known since every queued task is a media message) **truncated to its tail 48 chars** (`_truncate_tail`, 2026-09-07)：相册长 caption 名 250+ 字符，20 条就 ≈3041 字符，~27 条会撞 Telegram 4096 上令编辑直接报错；区分性最强的原文件名在名尾，故尾部保留、省略号打头，短名原样。Plus a source line: channels/supergroups get a clickable `https://t.me/c/<id>/<msg_id>` link (`message_link()`, only `-100…` ids are linkable — private chats and Saved Messages have no link format and show `#msg_id` instead). `/progress` appends the same link per in-flight download (`register_download(..., link=…)`). Replies render URLs as plain text with `link_preview=False` (no markdown escaping needed).

**Auto-clean subsystem.** `cleanup_loop` runs as a background task; when the interval changes via `/setcleartime`, `CLEAR_TIME_CHANGED` (`asyncio.Event`) is set to wake the loop immediately instead of sleeping the full old interval. `is_cleanup_message` decides what to delete — and **first returns `False` for any real, downloadable media** (`is_downloadable`): whitelist-relayed copies and parse-bot video replies retained in Saved Messages are never deleted, even when their caption contains a douyin/IG URL or a notification prefix. For non-media messages it matches exact command texts, `/setcleartime` commands and replies, any text containing a Douyin URL (the plain-text link *instruction* messages are still cleaned — a web-page-only preview has no file and is not "downloadable"), or text starting with one of the `CLEAN_NOTIFICATION_PREFIXES` — so normal user content is never deleted. The auto-clean task only deletes messages older than `CLEAR_MESSAGE_AGE_MINUTES`, while manual `/clearmsg` has no age limit and deletes the invoking command message last. Note the latency: a message survives up to (interval + age threshold) before auto-deletion (defaults 1 + 1 = up to 2 minutes). Each cycle also runs `cleanup_bot_chat_once()` for the bot menu chat (same interval). `cleanup_saved_messages_once` logs a heartbeat line every cycle even when nothing is deleted, and both cleanups also run once immediately after startup. **清理任务必须防「网络层取消」误杀（2026-09 实测修掉）。** Telethon 断线会对 pending 请求 future 调 `cancel()`，py3.8+ 的 `CancelledError` 是 BaseException、会绕开 `except Exception`：若它从清理的 fetch/delete 一路冒到 `cleanup_loop`，外层 `except asyncio.CancelledError` 会记「🛑 已停止」并 re-raise——主客户端中途掉线正撞上清理在拉消息时，自动清理任务当场被永久杀死、进程却照常重连下载，收藏夹的 开始/完成 通知自此无人清理（实测如此，非误报）。故清理的每个网络请求都经收口 `_shielded`（2026-09-11 提取为共享的 `netio.shielded`：请求放进子任务、结局一律经 `result()` 读取——子任务以 `CancelledError` 收场 = 网络层 → 记日志返回 `None`（本轮跳过、下轮再试）；只有清理任务本身被真取消（停服）才在 await 处原样传播；顺带用超时兜住僵死连接）。与 queue/download 对取消息、传字节的同类处理一致，详见上方「网络层取消」段。

**Logging.** A shared logger named `tg_userbot` lives in `log.py` and is configured once at package import (`log.configure(LOG_FILE, LOG_RETENTION_DAYS)`, idempotent — clears old handlers, so repeat imports don't stack). It writes to console and to the **daily-rotated** file handler in `RUNTIME_DIR/download.log` (a `TimedRotatingFileHandler` with `when="midnight"`, `backupCount=LOG_RETENTION_DAYS=7`: every midnight the current file rolls to `download.log.<YYYY-MM-DD>` and only the last 7 days are kept). `log.py` is a leaf — it must not import `config` (config imports it; retention is passed in as a parameter). `current_log_path()` returns the active base path (stable across midnight rotation) for `/logpath`/status display. **全链路 [T=] 追踪:** every queued download's `execute_queued_task` calls `log.set_trace(record["id"][:8])`, which writes the trace into a **contextvar** — it propagates automatically through every `await` (取消息 → `download_file` → 进度/失败/成功日志 → 通知) without global state, and each concurrent task has its own context so parallel downloads don't cross-contaminate. `_TraceFormatter` re-reads the contextvar at each format and inserts `[T=xxxx] ` before the message body when a trace is active (no trace → no prefix); the same formatter is shared by console + file handlers. To trace one download end-to-end, grep the log for its `[T=]` tag — everything from fetch through bytes to final `os.replace` carries it. `clear_trace()` runs in the task's `finally`. Progress callbacks log every `PROGRESS_STEP` percent (config.py). Log lines are the primary debugging tool; the startup routine prints instructions on how to verify message reception (expect the "📨 Saved Messages 收到消息" line). Formatter/rotation/migration primitives are unit-tested in `test_log.py` and `test_config.py` (constructed standalone, without mutating the shared logger).

**Chrome Agent V1（2026-09-09，专用 Profile + CDP）。** 通过 Telegram 远程用 Mac 上的 Chrome 完成下载，落 `<SAVE_FOLDER>/TG Chrome Download/`。**背景**：Chrome ≥136 出于安全禁止在默认 user-data-dir 上开 remote debugging（本机 Chrome 152 实测 9222 无响应），2026-09-09 用户明确允许新建 Profile——Agent 使用**专用独立 Profile**（`~/tg_chrome_agent_profile`，持久复用），以 `Google Chrome --user-data-dir=<专用> --remote-debugging-port=9222 --remote-debugging-address=127.0.0.1` 拉起**第二个 Chrome 实例**，与用户正常 Chrome 并行、绝不触碰后者的 Profile/Cookie/会话（原计划的「重启当前 Chrome」逻辑因此整段不需要）。专用实例无登录态 → 需要登录的站点下载不了（公开直链可用）；不导出/注入 Cookie。

**架构与通信**：两个进程、三份文件，全在 RUNTIME_DIR（规格 24「不要两进程无保护同写一个 JSON」→ 各写各的、temp+os.replace 原子、只读对方）：
- `chrome_client.py`（User Bot 侧）：`/chrome_start`（spawn `python -m tg_userbot.chrome_agent`，幂等——PID 文件 + `os.kill(pid,0)` + `ps` 命令行校验三重判活，防 PID 复用误判；已运行则不重复拉起）、`/chrome_stop`（SIGTERM Agent，**Chrome 保持运行**）、`/chrome_status`（Agent/Chrome/CDP/任务统计）、`/chrome [目录/][#标注] <URL>`（URL 校验 http/https → `new_task_id()`（uuid hex，唯一）→ 写 `chrome_requests.json`（task_id→user/chat/message 映射，**User Bot 重启后仍能凭映射通知**）。Agent 未运行时只提示先 `/chrome_start`，不自动启动。**下载子目录**（2026-09-10）：解析规则是「最后一个 `/` 之后的 `#xxx` 是文件名标注，前面的全部是目录路径」——`/chrome A/B/#测试 URL` 落到 `TG Chrome Download/A/B/#测试 原文件名`；**头 token 不带 `#` 时整个就是目录**（`/chrome Hyuk/250630 URL` → `Hyuk/250630/`，2026-09-10 补——此前该写法被当成 URL 交去校验，回执「URL 无效：Hyuk/250630」，见下）；唯一例外是头 token 自身就是合法 http(s) URL 时按旧行为当 URL（否则 `/chrome <URL1> <URL2>` 会凭空建出名叫 `https:` 的目录）。`parse_chrome_submit()` 是 `/chrome` 唯一的解析入口（命令分发由 `is_chrome_dispatch()` 判定，它刻意宽——非法形态也要落到 handler 里回一句人话）。`download_subdir` 随 request 落盘、由 `claim_new_requests` 拷进 task 并**持久化**（Retry 因此天然保持原目录），下载/改名/恢复/结果通知全部走 `chrome_agent.get_task_download_dir(root, task)` 而非全局目录。子目录来自用户输入，`safe_subdir()` 做底线校验（拒绝 `..`、`.`、反斜杠），非法值一律退回根目录——绝不会写出 `TG Chrome Download` 之外。另起 `notify_loop` 后台任务轮询 `chrome_tasks.json` 终态 → 按映射发 TG 结果通知（notified_at 标记防重发）。
- `chrome_agent.py`（独立进程，**零 Telegram 依赖**——本地 JSON 通道替代规格原案的「独立 Telethon session」，避免独立 session 首次人工输验证码破坏全自动）：启动即 `ensure_chrome_cdp`（9222 已有 CDP 直接复用，否则拉专用实例轮询 `/json/version` 取 `webSocketDebuggerUrl`，超时 `CHROME_CDP_CONNECT_TIMEOUT`=60s 报错退出）→ `Browser.setDownloadBehavior{allow, downloadPath, eventsEnabled}` → 恢复遗留任务 → 主循环（轮询认领 requests 新任务 + `process_pending_tasks` FIFO 串行执行，一次只跑一个）。**任务状态机**：PENDING → RUNNING → SUCCESS / RETRY_WAIT（到期自动重跑）→ FAILED（`CHROME_DOWNLOAD_RETRIES`=3 次全败，attempts/error 永久保留）；单次尝试超时 `CHROME_DOWNLOAD_TIMEOUT`=1800s。**启动恢复**（规格 27/28）：RUNNING（旧进程遗留）先查下载目录——filename 已记且成品存在（无 .crdownload）→ 直接补 SUCCESS 防重复下载；无法可靠判断 → 回 PENDING 重下；SUCCESS/FAILED 终态绝不自动重跑。**下载判定全靠 CDP 事件**（规格 22）：`downloadWillBegin` 记 guid/建议文件名，`downloadProgress` completed → 查成品文件记精确 bytes（size_bytes 原始保存）/ canceled → 失败；超时内无终态（含网页类 URL 不触发下载）→ 失败。SIGTERM 优雅停：RUNNING 原样持久化，重启走恢复。**CDP 稳态重连（2026-09-10 补）**：旧实现只在启动时连一次，WS 断了（Chrome 崩了/被关掉）之后整个进程生命周期都不再连——`next_event` 再也等不到事件，之后**每个**任务都只能干等到超时（单次 30 分钟），一夜白跑。现在 `ChromeCDPClient.is_alive()` 按「WS 在 + 接收循环未结束」判活；`next_event` 把「等事件」与「接收循环」一起挂上去、谁先结束用谁（连接死了**立刻**返回 None，不再干等满 timeout，队列里已攒下的事件仍优先返回）；`run_download_attempt` 见 None 且连接已死就当场失败（错误文案「CDP 连接已断开」）转 RETRY_WAIT（不是 FAILED），主循环每圈判活、断了走 `ensure_cdp_client()` 重连（专用 Chrome 还活着就直接复用其 CDP，否则重新拉起），重连失败按 `config.CHROME_CDP_RECONNECT_DELAY_SECONDS`(5s) 重试且**不退出进程**。
- 第三份：`chrome_tasks.json`（Agent 独占写，状态机事实源；**不依赖日志文本判断任务状态**，日志仅供排查，任务级 `[T=taskid8]` trace）。
- **日志**：Agent 在 `agent_main()` 入口调 `configure_agent_logging()` 把日志切到自己的 `runtime/chrome_agent.log`（`CHROME_AGENT_LOG_FILE`），**绝不与主进程共用 `download.log`**——两进程各持一个 `TimedRotatingFileHandler` 写同一文件时午夜会互相覆盖归档（2026-09-10 实测丢了整天日志，见 issues/001）。该调用**不能放模块级**：主进程也 import 本模块（`chrome_client.py:21` 用其纯函数），模块级 configure 会把主进程日志一并劫走。

**任务取消（2026-09-10，任务书 `docs/Chrome任务取消功能_DeepSeek开发任务书.md`）。** 新增终态 `CANCELLED`：任务记录**保留不删**（`tasks.remove()` 是禁止写法）、`attempts` 不因取消增加、`claim_next` 永不认领（它只领 PENDING / 到期的 RETRY_WAIT，天然跳过）、重启恢复也不碰终态。入口三条：`/chrome_tasks` 只列可取消的（顺序 进行中 → 排队 → 等待重试，组内保持文件顺序，序号即下标+1），`/chrome_cancel <序号|task_id>` 取消，主菜单【🌐 Chrome 任务】（按钮菜单里每条任务一个 🛑，直接带 task_id），三者共用 `chrome_client.cancelable_tasks()`/`request_cancel()` 保证不错位、回执一致。IPC 沿用文件通道：User Bot 独占写 `runtime/chrome_cancel_requests.json`（原子写、只留最近 200 条），Agent 只读并**幂等**应用。排队/等待重试的取消在认领前落地（`apply_cancellations`，只处理这两种状态）；**RUNNING 的必须真正中止 Chrome 下载**——`process_pending_tasks` 起一个并行 watcher 读取取消文件、置位 `asyncio.Event`（文件 IPC 只能轮询，这是全流程唯一的轮询点，下载判定仍是 CDP 事件驱动），`run_download_attempt` 用 asyncio 任务等待把「下一个 CDP 事件」与「取消事件」赛跑：取消赢就发 `Browser.cancelDownload{guid}` 并提前返回，返回值形状不变（调用方看着自己传进来的 `cancel_event` 判定是不是取消）。`Queue.get()` 被取消不消费队列，故 `downloadWillBegin`/`downloadProgress` 一个都不会丢；两边同一拍完成时**优先采纳事件**——完成先被确认就保留 SUCCESS，不因同时到达的取消改判。取消比 `downloadWillBegin` 先到时进宽限窗口等 guid 再取消（`config.CHROME_CANCEL_GUID_GRACE_SECONDS`，5s——太短会错过「点开链接后隔几秒才开始下载」的站点，取消就只剩改状态、文件照样落地），等不到说明下载没开始，由收尾的 `close_tab` 兜底（绝不允许「guid 未产生就取消失败」）。取消后 `clean_partial_download` 只删**本任务自己目录里的** `<文件名>.crdownload`（文件名未知就不动手，只记日志），绝不 glob 目录，绝不碰成品；随后 `process_pending_tasks` 回到认领循环继续跑下一个任务——取消单个任务绝不停 Agent（`stop_agent`/杀进程是禁止写法）。通知复用 `notify_pending_results`（`🛑 Chrome 下载已取消` 与成功/失败同列**持久**通知，避免重演「通知 85 秒后被自动清理」），`/chrome_status` 统计加「取消」一栏。**序号会漂移，ID 不会**：`/chrome_cancel` 除序号外还接受 **task_id 前缀**（≥6 位十六进制，`/chrome_tasks` 每行都打了短 ID）——序号是在按下取消的瞬间按当前列表重新解析的，若这期间前面的任务刚好完成、列表前移，同一个序号会落到另一个任务上（实测：`[1 进行中, 2 排队]` 里 1 跑完后，`/chrome_cancel 1` 指向的是原 2 号任务），按 ID 寻址则始终命中同一个任务。写盘失败时回执如实报错（`save_cancellations` 返回成败，与 `save_requests` 的「失败仅告警」刻意不同——取消是单向通道，谎报「已提交」等于什么都没做）。启动时另有一次孤儿半成品清扫（`sweep_orphan_partials`）：取消到达时下载还没开始、宽限窗口内也没等到 `downloadWillBegin`，任务已 CANCELLED 而文件名未知，**之后** Chrome 才开始的下载会留下没人认领的 `.crdownload`——该函数只删「不属于任何非终态任务**且** mtime 超过 10 分钟」的 `.crdownload`（看 mtime 是因为 `/chrome_stop` 只停 Agent、Chrome 还活着，上一个进程发起的下载可能仍在写盘），绝不碰成品。历史终态任务也在这个循环里裁剪（`trim_terminal_tasks`，保留最近 `config.CHROME_TASKS_KEEP_TERMINAL`=200 条，只裁最老的——刚终结的必须留着，通知是 5s 轮询发的）。
**权限**（规格 35）：chrome 命令仅 Saved Messages + sender==owner（`tg_secrets.json` 的 `chrome_agent.owner_id` 可覆盖，缺省回落主账号 MY_ID）；Agent 进程无 Telegram 面，请求文件只有本机 User Bot 可写。**回复前缀**「🤖 Chrome」「✅ Chrome 下载完成」「❌ Chrome 下载失败」与 `/chrome*` 命令进自动清理白名单。新依赖：`websockets`（CDP WebSocket，纯 Python；Termux 侧同样 `pip install websockets`）。**单测**：`tests/test_chrome_agent.py`（87 个：纯函数/持久化/状态机/认领/恢复五规则/重试流/CDP 事件归因注入 FakeCDP/启动参数无 0.0.0.0/wait_for_cdp/下载子目录/任务取消（CANCELLED 状态机、取消请求应用幂等、半成品绑定清理、CDP 取消与 guid 宽限窗口、历史终态裁剪））、`tests/test_chrome_client.py`（64 个：命令解析/下载目录/requests 读写/PID 过期与复用/进程启停（真 sleep 进程）/owner 校验/四命令流/通知幂等）。真机验收项：专用 Chrome 拉起、CDP 握手、真实 URL 下载、`/chrome` 全流程。

## 磁盘回收：媒体经 CloudDrive2 自动搬到 115（外部配置）

本地磁盘只留最近下载，**已完成的媒体**由 CloudDrive2（CD2，本机 WebDAV 端口 19798）的「备份」任务自动搬到 115 后删本地。这不是本仓库代码逻辑，是 CD2 侧的一次性运维配置，记录如下便于日后对照/排查：

- **备份任务**：源 = `SAVE_FOLDER`（macOS 为 `~/Downloads/Nagram`，CD2 界面里可能把家目录显示成 `/用户名/Downloads/Nagram`，以能看到媒体子文件夹为准）；目标 = `115open/Nekogram`。
- **文件过滤 = 白名单·扩展名**（媒体才搬，运行时文件天然免疫）：
  视频 `mp4 mkv mov avi wmv flv webm ts m4v mpg mpeg 3gp`；图片 `jpg jpeg png gif webp heic bmp tiff`；音频 `mp3 m4a ogg opus flac wav aac`。
- **完成规则 = 删除源文件**（真正释放磁盘的开关）。其余开关务必按此设置，顺序是先配好白名单再开删除源：
  - **删除规则（本地删文件时云端怎么处理）= 保留**。绝不能选「回收站/同步删除」，否则本地任何删除会把 115 上的备份副本一起清掉。
  - 替换规则 = 跳过（或历史版本）；实时监控开启。

安全原理：白名单外的一切（`download_history.txt`、`download_queue.json`、`download.log`、`whitelist_config.json`、`thread_config.json`、`clear_time.json`、`*.download`）不会进入备份 → 没有「完成后」→ 删除源规则不会碰它们。自运行时文件归集后这 7 个文件都挪进了 `SAVE_FOLDER/runtime/` 子目录（仍非媒体扩展名，免疫规则不变；CD2 备份源是 `SAVE_FOLDER` 递归范围，`runtime/` 在范围内也一样不被碰）。**设计取舍**：zip/rar/pdf 等归档类不在白名单，会一直留在本地（若也转发大量归档，磁盘仍会缓慢增长，需手动加后缀进白名单）。校验过一次真实运行：频道子文件夹的媒体被搬空删净、6 个运行时文件健在。（`Douyin/`、`Instagram/` 是统一下载链路重构前的历史目录，新版本不再写入——抖音/IG 视频现随普通下载落转发来源目录，如 `@DouYintg_bot 显示名/`，仍在 `SAVE_FOLDER` 递归备份范围内。）

**.download 半成品**：中断的下载重启后是「删半成品、整文件重下」，不支持断点续传（见下载模式）。残留的 `.download` 不会被 CD2 搬走。清理由 `clean_temp_files()` 负责，入口有二：`/clean` 命令/bot 菜单「🧹 清理」按钮，以及 **每次启动时 `main()` 会先自动清一次**（此时尚无下载，安全）——自动清理专门兜住异常退出、`/queue del` 后无人认领、或命名逻辑跨版本变化留下的孤儿 `.download`。正常情况下重启能自愈（任务已持久化，重下前删同名 temp）；出现孤儿 `.download` 的成因为：队列任务被移除后旧 temp 无人认领——原消息已删除被自动终结、`/queue del`/`/retry del` 清掉任务，或**命名逻辑跨版本变化**导致重启后算出的最终名与旧 temp 不同（实测案例：无 caption 文件旧版名 `未命名文件 - X.mp4`、新版名 `X.mp4`，旧半成品成死文件）。

**【启动/停止 CD2】bot 菜单按钮**（本仓库代码）：owner 在 bot 私聊主菜单点【🖥 启动CD2】/【🛑 停止CD2】。
- 启动 → `handle_menu_action` 的 `cd2` 分支调 `cd2_start_or_status()`：先 `cd2_is_running()` 对 `127.0.0.1:<port>` 做 TCP 探测，已运行 → 回「✅ 已在运行，无需重启」；未运行 → `cd2_launch()` 用 `start_new_session=True` 脱离本进程组后台拉起 CD2 独立程序（不会随 userbot 退出被连带终止），随后轮询端口约 30s 并回结果；启动输出追加到 `SAVE_FOLDER/cd2_launch.log`。CD2 无内置 stop 命令、未知参数会当普通启动参数（端口被占即自爆退出、不会双开），故停止走信号。
- 停止 → `cd2_stop_or_status()`：`_cd2_pids()` 用 `ps -eo pid=,command=` + **完整路径开头**匹配（`_cd2_pids_from_ps_output`），只命中配置 command 的可执行文件（主进程 + `Start-Service` 子进程），天然避开系统 iCloud 同名进程；对命中 PID 发 SIGTERM 优雅退出，轮询管理端口最长约 20s，仍开放则对余下 PID 补 SIGKILL。CD2 是“主进程 + `Start-Service 子进程`”一主一子结构（监听 19798 的是子进程）——按可执行路径整棵停最稳，不依赖具体 PID。
- 路径/端口/日志目录：CD2 独立程序路径与数据目录都含本机用户名（如 `~/software/clouddrive-2-macos-aarch64-1.0.10/clouddrive`、`~/Waytech/CloudDrive2/log`），属机器相关敏感配置 → 只能放 gitignore 的 `tg_secrets.json` 的 `cd2` 段（`command`/`port`，默认 19798；`log_dir` = CD2 数据目录下的 log，默认空 → 【备份记录】提示未配置），不入代码不入库。
- 【🗂 备份记录】 (`bak`)：读 `log_dir/backup.<日期>.log`，解析 `handle_localfs/cloudfs_notify: delete file and remove from all dests "<路径>"` 逐文件行（即文件已同步到 115、按完成=删除源从本地删除的那一行），过滤成媒体扩展名、按路径去重、时间倒序展示最近 N 条。同一文件因 cloudfs/localfs 双通知会出现两次，须去重；日志无文件大小字段。纯函数 `parse_backup_log_lines`/`read_cd2_backup_records`/`backup_records_text`/`cd2_log_dir` 与 PID 匹配等单测在 `test_menu.py`；端口探测/拉停/拉起/日志读取的真实流程靠实测。
